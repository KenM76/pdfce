# Memory index — pdfce-spec-librarian

- [Spec source extraction toolchain](reference_spec_source_extraction.md) — how to get text out of staged spec PDFs: **★★ TWO NEW ITEMS 2026-08-24 — (4f) A FIGURE CAN BE THE ONLY COMPLETE STATEMENT OF AN ALGORITHM (ISO 32000-2 Annex P’s prose omits two branches Figure P.1 has); the tell is a whitespace-stripped 0-hit on a phrase you can SEE — count an annex’s `/XObject` images before calling it read. (4g) PDF/X is the HARDEST paywall: iteh has no sample, `r.jina.ai` does NOT defeat `iso.org/obp`, and **veraPDF has `PDF_A`+`PDF_UA` ONLY** — the role-brief’s “use veraPDF’s rules” substitution is FALSE for ISO 15930; use ISO 32000-2 §11.4.7 NOTE 3 + pdfa.org + gwg.org instead.** · **★★ TWO NEW ROUTES 2026-08-21 — (4d) when an origin 403s AND its Wayback snapshots are the Cloudflare CHALLENGE PAGE (check the body, not the code), `https://r.jina.ai/<url>` fetches it — HTML *and PDF*, and it also defeated iso.org's committee catalogue; label the text proxy-extracted (typography normalised, bytes unstageable). (4e) The FREE machine-readable PDF registries — `pdf-association/pdf-extensions` JSON (use its GIT HISTORY for dates; its own `date` field was 5 months stale) and `adobe/pdf-names-list`'s prefix CSV (prefixes are CASE-SENSITIVE: `PDFa` ≠ `pdfa`); plus 4 raw-source fetches (pdf.js/pdfium/MuPDF/ghostpdl) = a full vendor-conformance table.** · **★★ TWO ROUTES 2026-08-20 — (4b) `grep -c` UNDER-COUNTS MULTI-WORD PHRASES because the extractor injects intra-word spaces (`s hall not invoke`); measure against a WHITESPACE-STRIPPED copy — 4 counts were wrong in one session and the misses were the HEADLINE EVIDENCE; plus a match-locator mapping a stripped hit back to a line, and the rule never to infer a clause number from line PROXIMITY. (4c) **A FIGURE MAY BE A RASTER IMAGE** — ISO 32000-2's Figure 9 is a JPEG, `pdfminer` returns zero glyphs, so extract it with `pypdf`'s `page.images` and **`Read` the PNG** (it settled `Do`-not-legal-inside-`BT` for 2.0).** · **★★ TWO ROUTES 2026-08-17 — (1) W3C specs are free/fetchable/quotable at `w3.org/TR/<shortname>/` (this is how a "how does PDF differ from Skia/browsers?" question gets ANSWERED rather than refused); (2) COMPILE AND RUN A DEPENDENCY — a 15-line `cargo` project pinned to the lockfile version, `--offline`, diffed against a spec reference, then root-caused by re-implementing the crate's own variant. Also 4a-sexies: some math symbols (`√`, `| |`) are PATHS, invisible even to the per-glyph pass — detect by an unexplained x-GAP.** · **★★ AN ERRATUM'S REVIEW `/State` IS A CHAIN, NOT A REPLY — walk `/IRT`, take max `/M`; 68.7 % of `Accepted` annots carry a later `Completed` (item 3-ISO2-ter), and the page-break interleave that puts one table's rows inside another clause's span can invert an answer (item 4c-bis)** · **★ ISO 32000-2:2020 IS NOW STAGED (`_sources\ISO_32000-2_sponsored_EC3.pdf`, 1023 pp, cached dump `C:\tmp\iso32000_2_dump.txt`) — and ITS ERRATA ARE ANNOTATIONS, so `extract_text()` returns the UNCORRECTED standard; recover struck text via `/QuadPoints` + `pdfminer` (item 3-ISO2)** · pypdf + the cached 756-page dump, pdfminer `LTChar.x0` for equations/figures/layout **plus the per-glyph `(x0,y0,size)` second pass that separates superscripts from fraction numerators, the mandatory arithmetic cross-check, source glyph dropouts (4a-bis/ter/quater), and **the `Annex<2 SPACES><LETTER>` heading artifact that makes `grep "Annex B"` a 0-hit false negative (4a-quinquies; TOC lines 129–190 give every annex's normative/informative marker)**, verified free URLs (ITU-T, Adobe, MS-OpenType, W3C), the Wayback route and its silent truncation, **the `pdfa.org`-403-but-Wayback-serves-its-PDFs route (decks + application notes ARE reachable)**, **the `cdn.standards.iteh.ai` FREE PREVIEW route for paywalled ISO standards (front matter = TOC + Introduction + clause 2 normative references, enough to settle cross-edition clause renumbering) and the errata site's per-clause TOC as a renumbering map**, the `pdf-issues.pdfa.org` host split, dead/gated URLs, vendored-crate source as an offline check, and the EMPIRICAL route (build a synthetic PDF, render it with `pdfce-cli`, probe the PNG).
- [PDF_Spec corpus conventions](project_corpus_state.md) — **56 numbered items; read the ones matching your dispatch shape.** **★ NEWEST (item 56): the MEASURED-DIVERGENCE dispatch — “we disagree with Acrobat on a real file; does the standard support the fix?” The honest verdict was “you are ALREADY CONFORMING, and the answer is EDITION-DEPENDENT”; the clause the dispatch named (§11.7.2) was the wrong one and its TITLE is why; the framing carried a false premise (a page with no `/Group` still HAS a page group, by a `shall`); a FIGURE held two branches the prose omitted; the strongest normative hook was a DIFFERENT MECHANISM in an uncited clause (§10.8.3’s colourant set, not a blending space); the “is it non-conforming?” half had a PRESCRIBED answer, not a silence; the actual lever was a gate-predicate mismatch already sitting in the corpus as a dormant ambiguity (`SP-A3`); **the test file’s CONFORMANCE LEVEL changed which imaging model applies — PDF/X-3 FORBIDS live transparency**; and a present, explicit scope banner was still partitioned on the wrong axis.** · **★ (item 55): the STANDARDISATION-STATUS dispatch — the operator's "when X becomes part of the standard" contains a PREMISE and auditing it is the deliverable; it was wrong in BOTH directions (no ISO work item AND already published as an industry extension two days earlier); measure a negative THREE structural ways plus the errata ANNOTATIONS; a false citation with a plausible clause number was in every search summary; a publisher's `SHALL` is not ISO's (new `spec: pdf_extension`); four raw-source fetches found three vendor divergences from a two-day-old spec.** Covers: prefix splits (content-origin + licensing axes) · gap/verification/ambiguity/retraction vocabulary and markers · SCOPED and SUPERSEDED-IN-DEPTH banners · axis re-audits (direction, depth, new index key) vs new files vs derived `ref` consolidators · evidencing NEGATIVE RESULTS with term-frequency counts · **everything a dispatch asserts is a hypothesis** (clause numbers, table numbers, flag bit values, named spec rules, dependency-crate behaviour, corpus-coverage claims, asserted contradictions, asserted guarantees) · reading FIGURES as geometry and worked EXAMPLES as oracles · modality ladders (`shall`/`should`/`may`, the hollow `shall`, the outcome-bound/method-deferred `shall`, the negative wiring constraint) · key-name overloading and sibling-row contrast/divergence · errata clusters and the 2.0-change-record permanence test · licensing refusals and the ambiguity-settings register · **adjudicating a clause-number conflict (0-hit the DISPUTED number; your own headings are reconstructed, not evidence) and the three leak paths for stale status claims (heading → index hash → bare product name)** · **verbatim-column/partition dispatches (the derived list is what's under test; the spec's prose goes stale vs its own table in version cohorts; find the third list)** · **normative-formula/transform dispatches (constants survive recall, citations do not; the headline is WHERE THE SPEC STOPS — prove deliberateness with a sibling clause that specifies the analogue; an array's major order is provable from the clause's own EXAMPLE; one file vs three, and recording why)** · **"the table has no such column" dispatches — grep the standard's OWN normative annexes before believing a delegating sentence, recompute derived COUNTS programmatically, and ask whether a missing limit protects against anything reachable** · **★ "(see the Bibliography)" in ISO 32000-1 is a SYSTEMATIC ERRATUM — the targets are clause-3 NORMATIVE references; grep clause 3 (dump lines 340–486) before any informative-vs-normative claim, and rebuild a dead hollow-`shall` leg on the INVOCATION VERB** · **"is this claim still right?" dispatches on a BEHAVIOURAL NEGATIVE — the clause can renumber/rename across editions and a later standard can dissolve a hollow `shall`; a wanted spec ANCHOR can be affirmatively contradicted (⇒ pdfce invariant, not conformance); DEFINITION and NORMATIVITY can live in different tables in different clauses.** · **★ the NULL-DELTA dispatch (relayed findings already recorded): re-verify from the source anyway, the residue is the erratum's FAILURE MODE not its facts, `grep "Annex B"` returns 0 hits because headings are `Annex<2 spaces><letter>`, and a "corpus-wide" rule stranded in a session audit log must be promoted to the reading-discipline block.** · **★ the EDITION-AXIS residue (item 43): when a clause family is complete on one edition the gap is "is this clause NUMBER still valid?" — and in an errata source `[INS]` is a CORRECTION, not the standard as printed.** · **★ the SECOND NULL-DELTA (item 44): a dispatch re-asks an already-RULED question ⇒ audit the ruling's own EVIDENCE — re-run term-frequency counts (an unbounded clause span silently runs to EOF), search the MODALITY not the noun, and separate what an ERRATA source proves from what you claimed; duplicate a stranded edition map into the section a decision will cite; reader behaviour is 2 curls of permissive source and can contradict your ruling; a MEASURED behaviour can contradict a SOURCED claim — file both.** · **★ the PAYWALL-FALLS dispatch (item 45): a licensed PRIMARY arrives ⇒ new `license_basis`, the algorithm-vs-expression rule for PUBLIC code, quote-don't-interpret the licence, errata-as-annotations, and expect it to correct FOUR of your own claims.** · **★ the DELIBERATELY-NARROWED dispatch (item 46): honour the scope cut and LABEL the half you skipped in three places; when a brief and an existing corpus file disagree on an identifier the CORPUS usually wins; a brief demanding something "verbatim" that returns 0 hits document-wide means the headline IS the negative (the radial quadratic is NOT in either edition — the clause states a painting PROCESS); check the NEWER EDITION before filing an ambiguity (2.0 had already closed the predicted zero-length-axis gap); a NOTE in 1.7 can be a `shall` in 2.0; an informative NOTE can be the ONLY statement of a load-bearing rule; and 2.0's clean Unicode is an independent cross-check on a 1.7 scrambled equation.** · **★ the VERBATIM-FORMULA-TABLE + CROSS-SPEC dispatch (item 47): path-drawn symbols are a THIRD dropout class; a COUNT in the corpus is a hypothesis (wrong by 3, in 4 files, inherited by the dispatch); a correct CONCLUSION can rest on a WRONG PREMISE and the premise is what gets generalised; COMPILE AND RUN the dependency and root-cause it; a dependency wrong against both specs is a DEFECT not an ambiguity; every divergence the dispatch predicted was absent and the real one was unpredicted; a later EDITION can change an ANSWER, not just a number.** · **★ the CLOSE-YOUR-OWN-FLAGGED-GAP dispatch (item 48): an ESTIMATED term-frequency count is not a MEASURED one (`knockout` 21→61) and the missed loci WERE the headline; "can data-structure X represent clause C?" is answered by rewriting the formulas in the structure's terms + a per-case capability table; COMPILE-AND-RUN extends to validating an IMPLEMENTATION ROUTE (and the free extra probe found that a `tiny_skia::Mask` is a SHAPE input); a corpus file's own PREDICTION can be confirmed — record it in the OLD file; closing a SCOPED gap changes THREE things, and the third is the one-line SUMMARY the old file wrote about the clause it had not read.** · **★ the MODALITY-DECISION dispatch (item 49): when a dispatch turns on `shall` vs `should`, THE MODALITY ITSELF CAN BE VERSION-GATED — 1.7's hedged `shall not ORDINARILY` + a reader consequence in the bare indicative became 2.0's writer `should` + an explicit processor `shall`, and the later edition's SPLIT is ISO's own reading of the earlier text; a writer-side "must I clamp?" is answered by the READER's obligation (identical rendering ⇒ minimal-diff wins); default-value chains and containment chains are DIFFERENT GRAPHS; a missing-OPERATION negative needs a document-wide co-occurrence regex, not a clause-scoped term count (1 hit in 1.7, an informative NOTE that 2.0 DELETED); a figure can illustrate more nesting than any clause requires; and **the dispatch's cheap "just confirm this quote" item is where the corpus error was** — confirming Annex C.2's page-size `should` exposed that ISO 32000-1's Annex C is `(normative)` (the corpus said informative) and that 2.0 DELETED the page-size range outright.** · **★ the TWO-SOURCES-DISAGREE dispatch (item 50): when a corpus file and `ROADMAP.md` contradict each other, THE CORPUS'S OWN ONE-LINE COMPRESSION is usually the ancestor — `Intensity 0–2` (the en-dash was the RAG author's, ISO says "in the range 0 to 2") became a validator spec refusing a conforming `/I 1.5`; QUOTE the row, never compress it. The enumeration-vs-range idiom contrast is in the SIBLING ROW of the same table; grep the RECOVERY VERB (`tolerate` = 2 hits/edition) not the key name; **THREE of my own counts were wrong from `head`-truncated listings — `grep -c`, `^KEY`-anchor row counts, and re-measure before reporting**; a key name can be overloaded across two unrelated features (`/BE`, second instance after `/AS`); the same key in three tables can carry three different normative forces; and the "confirm rather than re-derive" item is where the omission was, for the third time.** · **★ the CONTAINER-SCOPE dispatch (item 53): "feature F works on container A, extend it to B" — the mechanics transfer in ONE LINE and the CONTAINER-LEVEL hazards are the whole deliverable, and the OLD mechanics file's ABSENT scope banner lies invisibly; **evidencing an OWNERSHIP negative is strongest when you find the sibling constructs where the standard DID write the exclusivity `shall not` (4 of them) and, better, the normative object class it BUILT for the situation (Annex F's "shared objects = objects referenced from more than one page")**; search the PHENOMENON's phrase not the negation's; an edition can DE-DEPRECATE (2.0 deleted 1.7's "this construct is obsolete" and kept the `shall`); a table ROW can move CATEGORY and change what is legal where (`q`/`Q` ⇒ legal inside `BT`…`ET` in 2.0 only); a dispatch's table PAIR can be a CATEGORY ERROR; the thing that "varies per invocation" may live in the OBJECT not the CALL SITE; the bare INDICATIVE is a fifth modality rung below `may`.** · **★ the SIBLING-CARRIER dispatch (item 52): "build clause C's file, parallel to C′'s" — the headline was a CORRECTION to the corpus's own record (an erratum's `/State` is an `/IRT` CHAIN; 376/547 `Accepted` are stale); a table NUMBER can name DIFFERENT TABLES in the two editions (1.7 T178 = polygon, 2.0 T178 = LINE); a `free_primary` row can be COPY-PASTE-CORRUPTED and the next edition printing it clean is the proof; a key can be VACUOUS in one edition and the later edition's ADDITION proves it; "must X bound Y?" can be a true negative whose real failure mode is in a DIFFERENT clause (§12.5.5 SCALES, it does not clip); a term with EXACTLY 1 hit per edition IS the headline.** · **★ the TWO-GAPS-NAMED-FROM-OUTSIDE dispatch (item 51): "clause X is ENTIRELY ABSENT" is a coverage HYPOTHESIS — the clause's content can already sit in a file named for a NEIGHBOURING clause; when a dispatch hands you an ID minted by a SIBLING corpus (`A52`), read the sibling's row first, because the refutation is usually in the OLDER edition it did not check (1.7 answers with a `shall` the ambiguity 2.0 created); **the evidence that settles a two-`should` tie is in a THIRD clause nobody cited**; a dispatch's clause TITLES can be wrong while its quotes are exact; **a version-delta negative can be true of the SENTENCE and false of the CONCEPT** (`reserved to name` 0 hits in 1.7, but `reserved name` 1 hit — re-grep a looser variant); a recorded negative saying "no ingestion closes it" is a PREDICTION and expires (`OP-N1` → 2.0 §10.8.3); renumbering can follow TWO rules in one clause; UNREMEDIED ≠ PERMANENT (errata page + zero page annotations = two cheap negatives); **quote the FREE edition of a sentence that exists in both, so it can go into PUBLIC code**; and a Bash heredoc breaks on spec punctuation — Write the script, then run it.** · **★ the THIRD NULL-DELTA (item 54): when a dispatch names an UPCOMING PASS, the residue is a corpus file whose analysis rests on the CURRENT implementation — `iso32000__ref__spot_colour_overprint.md` §A says "pdfce renders to an ADDITIVE display ⇒ no colorant matching, ever", and Pass 97.x's CMYK-native compositor INVERTS 5 of its rows (a PRESENT, explicit, honest scope banner is still lethal once the reader's device class changes underneath it); **a version delta can change a conditional's PREDICATE TYPE**, invisibly to a renumbering check (1.7 "native space **is not** `DeviceCMYK`" = identity → 2.0 "**does not include** CMYK device colourants" = inclusion ⇒ `OPM` is inert under 1.7 and LIVE under 2.0 on a CMYK+spot buffer); **a suspected cross-edition DELETION needs a whole-document stripped COUNT** — a bullet one line past a page break LOOKED deleted and was retained, while its structural twin genuinely went 1→0 on 4 phrases; a "moot for pdfce today" marker in the ambiguity register is a DATED claim a Pass can EXPIRE (`SEP-A1`); a re-audited negative can survive while an ADJACENT mechanism appears (`black point compensation` 0→24 hits, §8.6.5.9 — say why it does NOT rescue the argument); and **a walk-back that CONTRADICTS a confident guess is still right** — read the intervening prose (§8.6.8 restricts BOTH `d1` glyph descriptions AND uncoloured tiling patterns).**
- [Font + spec-data licensing patterns](project_embeddable_data_licensing.md) — what may cross into pdfce; data-vs-document, WIDTHS-vs-SHAPES, the document-embedding-only font-exception trap, and availability ≠ redistribution licence (Adobe Supplements are all-rights-reserved despite LEGAL.md §2).
