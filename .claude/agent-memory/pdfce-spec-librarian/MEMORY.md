# Memory index — pdfce-spec-librarian

- [Spec source extraction toolchain](reference_spec_source_extraction.md) — how to get text out of staged spec PDFs: pypdf + the cached 756-page dump, pdfminer `LTChar.x0` for equations/figures/layout, verified free URLs (ITU-T, Adobe, MS-OpenType, W3C), the Wayback route and its silent truncation, the `pdf-issues.pdfa.org` host split, dead/gated URLs, vendored-crate source as an offline check, and the EMPIRICAL route (build a synthetic PDF, render it with `pdfce-cli`, probe the PNG).
- [PDF_Spec corpus conventions](project_corpus_state.md) — **37 numbered items; read the ones matching your dispatch shape.** Covers: prefix splits (content-origin + licensing axes) · gap/verification/ambiguity/retraction vocabulary and markers · SCOPED and SUPERSEDED-IN-DEPTH banners · axis re-audits (direction, depth, new index key) vs new files vs derived `ref` consolidators · evidencing NEGATIVE RESULTS with term-frequency counts · **everything a dispatch asserts is a hypothesis** (clause numbers, table numbers, flag bit values, named spec rules, dependency-crate behaviour, corpus-coverage claims, asserted contradictions, asserted guarantees) · reading FIGURES as geometry and worked EXAMPLES as oracles · modality ladders (`shall`/`should`/`may`, the hollow `shall`, the outcome-bound/method-deferred `shall`, the negative wiring constraint) · key-name overloading and sibling-row contrast/divergence · errata clusters and the 2.0-change-record permanence test · licensing refusals and the ambiguity-settings register · **adjudicating a clause-number conflict (0-hit the DISPUTED number; your own headings are reconstructed, not evidence) and the three leak paths for stale status claims (heading → index hash → bare product name).**
- [Font + spec-data licensing patterns](project_embeddable_data_licensing.md) — what may cross into pdfce; data-vs-document, WIDTHS-vs-SHAPES, the document-embedding-only font-exception trap, and availability ≠ redistribution licence (Adobe Supplements are all-rights-reserved despite LEGAL.md §2).
