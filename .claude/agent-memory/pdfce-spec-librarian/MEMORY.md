# Memory index — pdfce-spec-librarian

- [Spec source extraction toolchain](reference_spec_source_extraction.md) — how to get text out of staged spec PDFs: **★ ISO 32000-2:2020 IS NOW STAGED (`_sources\ISO_32000-2_sponsored_EC3.pdf`, 1023 pp, cached dump `C:\tmp\iso32000_2_dump.txt`) — and ITS ERRATA ARE ANNOTATIONS, so `extract_text()` returns the UNCORRECTED standard; recover struck text via `/QuadPoints` + `pdfminer` (item 3-ISO2)** · pypdf + the cached 756-page dump, pdfminer `LTChar.x0` for equations/figures/layout **plus the per-glyph `(x0,y0,size)` second pass that separates superscripts from fraction numerators, the mandatory arithmetic cross-check, source glyph dropouts (4a-bis/ter/quater), and **the `Annex<2 SPACES><LETTER>` heading artifact that makes `grep "Annex B"` a 0-hit false negative (4a-quinquies; TOC lines 129–190 give every annex's normative/informative marker)**, verified free URLs (ITU-T, Adobe, MS-OpenType, W3C), the Wayback route and its silent truncation, **the `pdfa.org`-403-but-Wayback-serves-its-PDFs route (decks + application notes ARE reachable)**, **the `cdn.standards.iteh.ai` FREE PREVIEW route for paywalled ISO standards (front matter = TOC + Introduction + clause 2 normative references, enough to settle cross-edition clause renumbering) and the errata site's per-clause TOC as a renumbering map**, the `pdf-issues.pdfa.org` host split, dead/gated URLs, vendored-crate source as an offline check, and the EMPIRICAL route (build a synthetic PDF, render it with `pdfce-cli`, probe the PNG).
- [PDF_Spec corpus conventions](project_corpus_state.md) — **45 numbered items; read the ones matching your dispatch shape.** Covers: prefix splits (content-origin + licensing axes) · gap/verification/ambiguity/retraction vocabulary and markers · SCOPED and SUPERSEDED-IN-DEPTH banners · axis re-audits (direction, depth, new index key) vs new files vs derived `ref` consolidators · evidencing NEGATIVE RESULTS with term-frequency counts · **everything a dispatch asserts is a hypothesis** (clause numbers, table numbers, flag bit values, named spec rules, dependency-crate behaviour, corpus-coverage claims, asserted contradictions, asserted guarantees) · reading FIGURES as geometry and worked EXAMPLES as oracles · modality ladders (`shall`/`should`/`may`, the hollow `shall`, the outcome-bound/method-deferred `shall`, the negative wiring constraint) · key-name overloading and sibling-row contrast/divergence · errata clusters and the 2.0-change-record permanence test · licensing refusals and the ambiguity-settings register · **adjudicating a clause-number conflict (0-hit the DISPUTED number; your own headings are reconstructed, not evidence) and the three leak paths for stale status claims (heading → index hash → bare product name)** · **verbatim-column/partition dispatches (the derived list is what's under test; the spec's prose goes stale vs its own table in version cohorts; find the third list)** · **normative-formula/transform dispatches (constants survive recall, citations do not; the headline is WHERE THE SPEC STOPS — prove deliberateness with a sibling clause that specifies the analogue; an array's major order is provable from the clause's own EXAMPLE; one file vs three, and recording why)** · **"the table has no such column" dispatches — grep the standard's OWN normative annexes before believing a delegating sentence, recompute derived COUNTS programmatically, and ask whether a missing limit protects against anything reachable** · **★ "(see the Bibliography)" in ISO 32000-1 is a SYSTEMATIC ERRATUM — the targets are clause-3 NORMATIVE references; grep clause 3 (dump lines 340–486) before any informative-vs-normative claim, and rebuild a dead hollow-`shall` leg on the INVOCATION VERB** · **"is this claim still right?" dispatches on a BEHAVIOURAL NEGATIVE — the clause can renumber/rename across editions and a later standard can dissolve a hollow `shall`; a wanted spec ANCHOR can be affirmatively contradicted (⇒ pdfce invariant, not conformance); DEFINITION and NORMATIVITY can live in different tables in different clauses.** · **★ the NULL-DELTA dispatch (relayed findings already recorded): re-verify from the source anyway, the residue is the erratum's FAILURE MODE not its facts, `grep "Annex B"` returns 0 hits because headings are `Annex<2 spaces><letter>`, and a "corpus-wide" rule stranded in a session audit log must be promoted to the reading-discipline block.** · **★ the EDITION-AXIS residue (item 43): when a clause family is complete on one edition the gap is "is this clause NUMBER still valid?" — and in an errata source `[INS]` is a CORRECTION, not the standard as printed.** · **★ the SECOND NULL-DELTA (item 44): a dispatch re-asks an already-RULED question ⇒ audit the ruling's own EVIDENCE — re-run term-frequency counts (an unbounded clause span silently runs to EOF), search the MODALITY not the noun, and separate what an ERRATA source proves from what you claimed; duplicate a stranded edition map into the section a decision will cite; reader behaviour is 2 curls of permissive source and can contradict your ruling; a MEASURED behaviour can contradict a SOURCED claim — file both.** · **★ the PAYWALL-FALLS dispatch (item 45): a licensed PRIMARY arrives ⇒ new `license_basis`, the algorithm-vs-expression rule for PUBLIC code, quote-don't-interpret the licence, errata-as-annotations, and expect it to correct FOUR of your own claims.**
- [Font + spec-data licensing patterns](project_embeddable_data_licensing.md) — what may cross into pdfce; data-vs-document, WIDTHS-vs-SHAPES, the document-embedding-only font-exception trap, and availability ≠ redistribution licence (Adobe Supplements are all-rights-reserved despite LEGAL.md §2).
