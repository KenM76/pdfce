# Memory index — pdfce-spec-librarian

- [Spec source extraction toolchain](reference_spec_source_extraction.md) — pypdf (no pdftotext); "re-extract before reconstructing" (5/5); **a spec EQUATION scrambles in any linear dump — recover it by pdfminer `LTChar.x0` ordering (+ the 0-based `page_numbers`, `PYTHONIOENCODING=utf-8` and own-y-row-subscript gotchas)**; cross-check against a sibling table; ITU-T + Adobe + **MS-OpenType** fetch recipes, **the Wayback route + its silent 1-MiB truncation**, dead/gated URLs (adobe.com DAM hangs, pdfa.org 403, MicrosoftDocs raw 404), **vendored-crate source as an offline verification route**, license verification.
- [PDF_Spec corpus conventions](project_corpus_state.md) — prefix splits (content-origin **and licensing** axes); gap/verification/ambiguity vocabulary; retraction markers; re-auditing on a new AXIS vs building a MIRROR file; SCOPED banners; `NEGATIVE RESULT` + never closing one from recall; evidencing negatives with term-frequency counts; **a dispatch's clause numbering is a hypothesis — verify the source contains it**; **refusing to write an unobtainable algorithm is a valid deliverable**; **a WRITE audit of clause C must trace C's artifact to its CONSUMER clause — the binding write-side rule can be invisible from C alone (§8.10 tolerates a zero-area BBox; §12.5.5 forbids it)**; **one GENERATION dispatch splits by clause — appended axis section on already-covered clauses + a new file for the new-machinery clause; and flag-bit VALUES (not just clause numbers) are dispatch hypotheses to re-derive**; **behavioral negatives = the HOLLOW SHALL (spec `shall execute` but defers all semantics to external docs ⇒ recognize-never-execute conformant) + the NO-CLAUSE tool-verb (flatten has zero clause; deliverable names what constrains it) + shall-vs-may on producer flags (`/NeedAppearances` = `may`) + the OUTCOME-BOUND/METHOD-DEFERRED shall (redaction: spec judges the RESULT via four `shall`s, defers the procedure ⇒ removal = pdfce policy)**; **DESTRUCTIVE-op dispatch shape (Pass 8): pure-spec clause file + DERIVED removal-mechanics `ref` file; the op INVERTS corpus invariants (R35 vs minimal-diff; §7.5.6 incremental-safe→redaction-unsafe; save_full ObjStm-verbatim→removed-object-survives); the carrier-sweep is a deliverable; a Table-N cross-ref is a hypothesis (Table 192 cites 175, means 179 — erratum)**; **a dispatch's asserted SPEC GUARANTEE is a hypothesis too (Pass 12.M2): no §14.5 /PieceInfo-preservation rule exists, so the negative result targets the dispatch's PREMISE — split pdfce-invariant survival (R34) vs foreign-tool survival (empirical→personal_rag) vs spec, and MIRROR a private carrier into a reader-visible structure (/PieceInfo→/Measure §12.9) for interop; measurement unit algebra: /R is display-only, /C(first /X)=real-units-per-user-space-point=the scale; no §12.9.3 in 32000-1 (that's 32000-2); annotation /OC path is independent of content-stream BDC/EMC /OC**; **an APPEND/ORIGINATION dispatch (add NEW content, Pass 16.0): the binding rule can live in an UN-dispatched clause (q/Q array-balance = §8.4.2, not §7.7.3.3) — trace the operation to its INVARIANT clause; byte-identical = edit the /Contents REFERENCE not the stream; graphics state inits ONCE at page start so the appended run must self-set Tf/colour/Tm + self-wrap q/Q; the /Resources inheritance trap (never mutate a shared ancestor dict); PDF-2.0 /Widths answerable without a 2.0 source**; **an EMBEDDING/REDISTRIBUTION dispatch (Pass 21.0): a DEPENDENCY CRATE's asserted behaviour is a hypothesis — read the vendored source (a table can be emitted by a sibling module); when a dispatch frames a crate behaviour as a limitation, check whether the spec MANDATES it (§9.9 `cmap` shall-not-be-present); a CONSUMER-OBLIGATION question usually HAS an answer (OpenType `fsType`'s three `must`s; ISO 32000-1 §9.9's own licensing paragraph, invisible because the clause is famous for Table 126); a CROSS-SPEC BRIDGE GAP is a PERMANENT ambiguity; REWRITE-not-extend = whole-file retraction + sweep the derived files that inherited the error**; **a GEOMETRY-AUTHORING dispatch (Pass 12.M2 offset dimensions): a spec FIGURE is normative and readable as GEOMETRY — extract the page's content stream, walk the `cm` stack, reconstruct the paths, and refute the rival reading ARITHMETICALLY (one consistent figure scale vs two); state what the figure CANNOT settle; cross-check a second invariant while you have the coordinates (rigid translation ⇒ the dispatch's feared misreport is structurally impossible); quote verbatim any row whose meaning is CONDITIONAL on a sibling key's presence; the spec often already NAMES what the code invented (`Butt` = the hand-rolled tick) yet specifies no size ⇒ naming opportunity, not a bug; a relative-only definition (`ROpenArrow` = "reverse of `OpenArrow`", direction never stated) is a GAP; a domain's vocabulary can be 0-hit absent (`dimension line`/`witness line`) while the feature is fully expressible; and a dispatch can be entirely RIGHT — record the confirmation**; **a COLLISION/RE-INDEX dispatch (Pass 37.2 `/Ff`): the clause is ALREADY verbatim-covered and the finding lives in the JOIN of sibling tables — deliverable = a `ref` consolidator re-indexing existing material on a NEW KEY (bit position, not field type); answer the general question EXHAUSTIVELY (bit 26 confirmed unique, but bit 23 is same-name-different-precondition, a third category); re-indexing finds errors in the DERIVED PROSE beside a correct table ("bits 4–14, 18–20, 22–27" was wrong); a KEY NAME can be overloaded across dictionaries (`Ff` = 4 flag words, 3 nesting in one `/Sig` field); the spec sometimes ENUMERATES its own table family (Table 246 cites 221/226/228/230) = closure evidence; and the deliverable is a function SIGNATURE, `decode_flags(ff, ft)`**; **a NAMED-GAP dispatch where the gap was a recorded NON-GOAL (§12.7.3.4 rich text): closing it = `AMENDMENT` whose "what was wrong" is *"nothing, as a scope statement"* + sweep the 3 files that quoted it; the licensing split gets a THIRD, "partly" shape — ISO enumerates the DATA (PDF-1.5 grammar) and name-defers the BEHAVIOUR (XFA) ⇒ two files that cross-warn; grep clause 3 vs the Bibliography yourself because the citing prose was WRONG (XFA is Normative, not bibliographic) so a "spec is silent" negative is closable normatively-by-reference; a `should` can carry the OPPOSITE engineering weight — also ask WHICH KEY THE OUTPUT IS BOUND TO (`/V` is a `should` mirror, `/RV` is what the appearance reads ⇒ stale `/RV` = corruption); an ERRATA CLUSTER (8) and an erratum EXPLAINED by an earlier build (E1's "Table 226" is the bit-26 collision leaking into prose); verify a suspected source TYPO with pdfminer as a second opinion on pypdf; Wayback's silent-truncation boundary VARIES (5 MiB this time, 1 MiB before) and differs per snapshot PATH — check `%%EOF`, and `wayback/available` 429s; and a licence can permit BUILDING the feature + copying the GRAMMAR while forbidding the PROSE (read the Preface IP page, not the cover NOTICE)**; **a CLAIM-VERIFICATION dispatch (§8.7 pattern anchoring) has a FOURTH verdict — CONFIRMED-BUT-SCOPE-INCOMPLETE: the correction lands on the claim's implicit scope, not its truth (`sh` is CTM-relative, the exact inverse of a type-2 pattern, and the spec flags the contrast in its own "By contrast" parenthesis); "does it differ between X and Y" can be answered "the distinction does not exist" via two byte-identical table rows; a NOTE may be the only plain statement ⇒ never cite it alone, pair it with the `shall`; a cost estimate can be right about DIRECTION and wrong about LAYER (the ON branch is a resource-graph edit, not a content-stream edit); and confirming someone's sentence still costs a full clause read — it produced 2 new spec-ambiguity rows**; **a RENDER-PIPELINE dispatch on a WHOLLY-UNINGESTED clause (image transparency, clause 11): "clause C was never ingested" is a hypothesis about the CORPUS not the SOURCE — one grep settles it and leading with "the source has it, nothing is from recall" outranks any single answer; an existing "the spec is silent" AMBIGUITY ROW can be WRONG (§8.9.6 is silent on `/Mask` vs `/SMask`; Table 89 and §11.6.4.3 each state it) ⇒ an ambiguity scoped to one clause is provisional until the OWNING clause is read, and resolution = strike-through + `RESOLVED, NOT an ambiguity`; ONE KEY NAME can be TWO unrelated features (ExtGState `/SMask` dict vs image `/SMask` stream) and the corpus's own DERIVED ADVICE, not its tables, carried the over-read ⇒ `AMENDMENT` + a `§0` side-by-side disambiguation table; a POLARITY answer lives in a DEFINED-BY-REFERENCE clause the dispatch never named (§8.9.6.3 → §8.9.6.2) and stencil-0=PAINT vs softmask-0=INVISIBLE are exact opposites; and a restriction table can bound a key's DEFAULT without bounding its SEMANTICS or VALUE SET (Table 145's bare "Required"/"Default `[0 1]`") — unmentioned keys keep ordinary meaning, and one such omission was itself an ambiguity**; **a THREE-AREA PARITY-DRIVEN SCOPING dispatch (prepress): "a sibling librarian grepped zero hits" is a CORPUS fact not a SOURCE fact (2nd same-day instance); a clause can DECLINE to define its own subject (§8.6.7 "is not defined here" → §11.7.4 defines it) ⇒ a DECLINE/DEFINE file pair that cross-warns "read both or neither"; "does the standard MANDATE X or did the vendor choose it?" is answered by a MODALITY LADDER whose bottom row is "a `shall` binding a reader — DOES NOT EXIST" (output intents: §14.11.5 "free to disregard" + §8.6.5.7 NOTE 3 "may suggest" ⇒ **Acrobat chose it**); DEVICE-CLASS COLLAPSE — one property of the target (additive display) voids six `shall`s and is worth more than any clause detail; a new consolidator AXIS, OBLIGED (O1–O17) vs CHOOSING (C1–C8), which surfaced two unasked-for hazards (§11.7.4.4's fill+stroke knockout group; `/SeparationInfo` as a page-op correctness bug); and refusing a conformance matrix on LICENSING grounds — check clause 3 vs the Bibliography FIRST (ISO 15930 is Bibliography-only AND paywalled = item 27's rule with the opposite outcome), then give closure routes**; **a CONSOLIDATION-ONLY dispatch (no clause ingested, no file edited) re-indexing on a PRODUCT key ("does this ambiguity become an operator setting?") — MEASURE the population (a dispatch's count is an OCCURRENCE count: 155 findings ≠ 67 IDs); the biggest finding comes from a read-only grep of the CONSUMER CODEBASE (10 of 18 settings already hard-coded, `/QuadPoints` authored against §12.5.6.10); GRADE every default (a)Acrobat/(b)census/(c)documented-impl/(d)guess and expect (d) to be modal; BLAST RADIUS (RENDER free / EXTRACT = R35 correctness / BYTES = rule-3 constrained, whose right default is often "match the base file"); a NO-KNOB bucket is a deliverable and out-of-band changes the UI WORDING not the reading; mint new ambiguities register-local + a `## Corrections owed` section; and answer "where would a setting even LIVE?" — R15 doesn't exist**; **a DELETION/REFERENTIAL-INTEGRITY dispatch (Pass 38.5): the governing clause is a GENERIC one from another part of the standard and it INVERTS the framing (§7.3.10 "an indirect reference to an undefined object shall not be considered an error … treated as a reference to the null object" ⇒ every dangling annotation ref is LEGAL; `dangling`/`orphan` = 0 hits); **the conformance obligation for key A can live entirely in key B's row** (`/RT` is inert by its own text, but Table 170's `/IRT` row says "Required if an RT entry is present" ⇒ that's the only defect in the whole cascade); a DEFAULT VALUE makes "strip the key" a semantic change (`/RT` default `R` reclassifies a `Group` subordinate as a reply); **a cascade can make a SILENT ambiguity UNREACHABLE — say so instead of answering it**; a standard can FORBID and SPECIFY the same state (popup: `shall not appear alone` vs `/Parent` Optional vs NOTE 2's defined parentless reading — and the orphan starts speaking in its OWN voice); a NOTE's scope is set by the bullet it hangs under (the corpus had NOTE 2 scoped to markup annotations, it is the POPUP); and the PDF Association HOST SPLIT — `pdfa.org` 403s but **`pdf-issues.pdfa.org` errata + `pdfa-inc.org` serve fine**, the errata site confirms 1.7 errata AND yields 2.0 deltas, and **ISO 32000-2 has been $0 since 2023-04-05 (escalate, don't checkout; zero cost ≠ redistributable)****.
- [Font + spec-data licensing patterns](project_embeddable_data_licensing.md) — what may cross into pdfce; data-vs-document, WIDTHS-vs-SHAPES, the document-embedding-only font-exception trap, and **availability ≠ redistribution licence (Adobe Supplements are all-rights-reserved despite LEGAL.md §2)**.
