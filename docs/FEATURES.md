# pdfce — Feature status

**Deliberately concise — this is a scan, not a record.** `ROADMAP.md` is
organized by Pass (engineering order) and carries the full reasoning,
acceptance criteria, and history; a single capability here is often
built across several Passes there. This file answers one question only
— *what can pdfce do, and what's missing* — and a features list nobody
can scan in about two minutes has failed its job. Don't "fix" this file
by expanding it; if a row needs an argument, that argument belongs in
`ROADMAP.md`, not here.

**Maintained by `pdfce-librarian`, in the same filing as every
`ROADMAP.md` "Pass shipped" update** — when a Pass moves to Shipped,
its row(s) here get ticked in the same edit. Not a separate chore.
**`ROADMAP.md` is the source of truth; if this file and `ROADMAP.md`
ever disagree, `ROADMAP.md` wins** and this file is stale until the
next filing.

## Legend

- `[x]` — built and shipped.
- `[ ]` — not yet built.
- `—` — **not applicable**, not "not built." A row marked `—` in the
  `cli` column means the capability has no sensible one-shot batch
  form (e.g. "pan the canvas"); a row marked `[ ]` in the `gui` column
  means it genuinely has no GUI surface yet and is reachable only from
  `pdfce-cli` today. Confusing these two is exactly the mistake this
  legend exists to prevent — a `[ ]` is a gap, a `—` is a shape
  mismatch.
- **core** = `pdfce-core`/`pdfce-render` (headless, no GUI deps).
  **cli** = `pdfce-cli`. **gui** = `pdfce-gui`.
- **ce dimension** = a `/Line`+`/IT /LineDimension` annotation **pdfce
  itself authors** (scale, group, `/Measure`, `/PieceInfo` sidecar).
  **pdf dimension** = a dimension already present in the file from CAD
  or another tool — pdfce reads and measures against it but never
  authors or alters it. See `CLAUDE.md` rule 15; never read "dimension"
  below as the other kind.
- A handful of `pdfce-cli --help` subcommands (`to-pdfa`,
  `validate-pdfa`, `sign`, `bates-stamp`) are **Pass-0 stubs** that
  print "not implemented, later Pass" — they exist in `--help` but are
  not a shipped capability. Not ticked anywhere below; the features
  they represent are listed under *Planned*.

### The `Acrobat` column

Does **Adobe Acrobat Pro** (current 2026 subscription release) have this
**capability**? Never how its UI exposes it. Four states plus a partial,
and they are not interchangeable:

- `[x]` — Acrobat has it. `[ ]` — **verified absence**, not "we didn't
  find it." `◐` — Acrobat has only **part** of what the row names; the
  RAG file below says which part. `—` — **not meaningful to ask** (a
  pdfce-internal concept, or GUI mechanics the Acrobat RAG deliberately
  never catalogues). `?` — **not looked up yet.** Never read `?` as
  "Acrobat lacks it"; that is the `[ ]` state and only the `[ ]` state.
- **Authority, and the per-row basis this one-character column cannot
  carry:**
  `D:\Dev\Rag-Specialized\Acrobat_Features\comparison__pdfce_feature_column.md`
  (`last_verified: 2026-08-05`).
- **The verdicts are NOT uniformly confident, and must not be read as
  if they were.** Redaction, Forms, core page ops, Text and the
  measuring tools rest on dated `Acrobat_Features` files — trustworthy
  at `must_have` acceptance-criteria grade. **Digital signatures,
  encryption, Bates, OCR, accessibility, comparison, portfolios,
  optimization, PDF/A and prepress rest on a single 2026-08-05 web
  search** — provisional; re-verify before any acceptance criterion
  leans on them. Those ten buckets have **zero** dedicated RAG files
  (filed as backlog in `ROADMAP.md`). A few `[x]` are high-confidence
  inferences or carry material caveats — Acrobat's word-spacing and
  baseline-shift controls were removed from its own UI; its cross-block
  reflow is cloud-gated and did not exist before 2021 — and the RAG
  file carries those per row.

## Implemented

### Document & pages

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Open, parse, and render a PDF (Pass 1) |
| [x] | [x] | [x] | [x] | Recovers a damaged/malformed cross-reference table; tolerates common lenient-PDF defects (Pass 13a/13b, `/Contents`-defect fix, missing-`endobj` repair `49dfe81`) |
| [x] | [x] | [x] | [x] | Rotate pages, incl. refusal disclosure (Pass 3.2 core/CLI; GUI rotate buttons + `5b2682b` disclosure fix) |
| [x] | [x] | [x] | [x] | Delete pages (Pass 3.2 core/CLI; GUI `delete_pages` call in `main.rs`) |
| [x] | [x] | [x] | [x] | Reorder pages — thumbnail drag or keyboard (Pass 3.2 core/CLI; GUI `apply_reorder`/`move_pages_keyboard` in `main.rs`) |
| [x] | [x] | [x] | [x] | Merge / combine multiple files into one (Pass 3.2 core/CLI; GUI `merge_tool` add-files button in `main.rs`) |
| [x] | [x] | [x] | [x] | Split a document into multiple files, `EveryN` criterion only (Pass 3.2 core/CLI; Pass 3.4 GUI) — break-point and bookmark criteria still unbuilt, named in the panel |
| [x] | [x] | [x] | [x] | Insert pages from another file — whole source, Start/End only (Pass 3.2 core/CLI; Pass 3.5 GUI) — writes a NEW FILE, not an in-place edit; true in-place insert needs `EditSession::insert_pages` (core-only, unbuilt — see *Planned*) |
| [x] | [x] | [x] | [x] | Extract pages to a new file (Pass 3.2 core/CLI; GUI `PdfceApp::extract_selection`, `main.rs` ~L3601, reached via `Action::ExtractSelection` from a thumbnail-rail button ~L8936 — the GUI names it "extract selection," not "extract pages," which is why a name-based grep for `extract_pages` missed it; see R156) |
| [x] | [x] | [x] | ? | **Preseparated-file (`/SeparationInfo`) page-set integrity across Delete/Extract/Split/Merge** (`Pass 50.0`, `a083f70`, 2026-08-08) — the four page operations above (Pass 3.2) silently corrupted files carrying `/SeparationInfo`: delete left dangling page-array refs uncounted; extract/split's copier dropped the Required key entirely, reported only as an anonymous `dangling_references` tick. **Repairs the STRUCTURAL invariant** (drops a dangling page-array entry) **and REFUSES to guess the SEMANTIC one** (which surviving pages should now be named) — a reading of the existing no-repair posture (R27/rule 4), not a reversal. Disclosure NAMES the affected colorants, unlike the neighbouring dangling-reference count. Found incidentally by `pdfce-spec-librarian` (O17) and fixed the same session (R170) |
| [x] | [x] | [x] | [x] | Edit document metadata (`/Info` dict) (Pass 3.2 `set-info`; GUI Properties dock panel) |
| [x] | — | [x] | [x] | Undo/redo command log (Pass 3.1 core; wired into the GUI at Pass 14.3) |
| [x] | [x] | [x] | [x] | Save (incremental-by-default or full-rewrite); **"Save a copy" only — no true in-place overwrite yet** (see *Planned*). **Full rewrite REFUSES a file whose highest object number exceeds `MAX_REWRITE_OBJECT_NUMBER` = 8,388,607** (Annex C Table C.1 max indirect objects; `WriteError::ObjectNumberTooLarge`, `0df6158`) — §7.5.4 makes a one-section xref table cost O(largest object NUMBER), so pdfium's 1.2 KB `bug_455199.pdf` (which names `2147483648 0 obj`) would need ~40 GB of entries and used to grind for about an hour. **A refusal, not a repair** — emitting a sparse table would be malformed, renumbering would break byte-identity. **Reading such a file is unaffected** (`inspect`/`extract-text` both work); the bound refuses nothing a conforming producer can write, since 2³¹ exceeds Table C.1's integer maximum. **A full rewrite also DROPS any bytes before `%PDF-`** (`fa4f83c`) — veraPDF reads xref offsets as *header-relative* whenever a preamble exists, so every preamble-preserving file pdfce wrote was unreadable to it even with offsets absolute and correct; emitting the header at byte 0 makes both readings the same number. **Incremental and identity-append saves still preserve the preamble** (they promise byte identity / a byte prefix; a full rewrite promises only per-object identity). See `ARCHITECTURE.md` §5.6.1 |
| — | — | [x] | [x] | **Multiple documents open at once** (Pass 39.0) — a parked list beside the active document; each parked document keeps its own unsaved edits, selection and scroll position. The switcher row appears only at 2+ documents. `cli` `—`: a one-shot batch tool has no "open documents". **Restriction:** `redact_search_query` is app-level, not per-document, so a redaction search query follows you across a switch |
| — | — | [x] | [x] | **Close a document, with a three-way save prompt** (Pass 39.1) — save-and-close / close-without-saving / keep-open. **Save-and-close only closes if the save SUCCEEDED** (a cancelled dialog or refused write leaves the document open); a clean document closes with no prompt at all |

### Text

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Extract & copy text: search index, `ToUnicode`, reading order, plus page/document-scope clipboard copy (Pass 4 core+CLI `extract-text`; GUI Copy ▾ menu, `CopyScope::Page`/`Document`) |
| [x] | — | [ ] | [x] | Select text on the canvas and copy the selection — the selection itself exists (Edit Text tool: triple-click, `text_caret_after_click`, a live caret/anchor pair), but there is no `CopyScope::Selection` and no copy verb reaches it. `cli` marked — (not `[ ]`): "mouse-select a range, then copy" has no batch shape; page/document-scope copy above already covers the CLI case |
| [x] | [x] | [x] | [x] | In-place text editing of existing runs (Pass 14.x) — arrow-key caret navigation fixed `7d368e6`: the canvas claims the arrows via `set_focus_lock_filter` instead of letting egui steal focus leftward |
| [x] | [x] | [x] | ◐ | Text formatting: size/colour/font-family-style, char/word spacing, horizontal scale, super/subscript, synthetic bold/italic (Pass 19.x, FF-H — complete end-to-end) |
| [x] | [x] | [x] | [x] | Within-block reflow incl. justified alignment (Pass 15.x, FF-A) — open defect: auto-detected wrap width can inherit a prior edit's overflow (Pass 33.0, disclosed but not yet fixed) |
| [x] | [x] | [x] | [x] | Add new page text: point insert + wrapped multi-line box (Pass 16.x, FF-D) |
| [x] | [x] | [x] | [x] | Edit composite/CID (`/Type0`) text runs (Pass 29.0) — the GUI's Edit-Text commit path (`commit_text_edit_draft`) calls `EditSession::edit_text` directly with no composite gate of its own, so it inherited the capability the moment core lifted the refusal; no GUI-side wiring was needed (see R156). **Exception:** word spacing (`Tw`) on a composite run is still refused (R91) — that gate is real and distinct from text-edit itself |
| [x] | [x] | [x] | **[ ]** | Add non-Latin text via a subsetted, embedded donor font (Pass 21.0 core/CLI; Pass 37.1 GUI — face picker in Add Text, supplied faces listed first, refusal-with-remedy, measured embed disclosure). glyf/TrueType donors only; **no shaping ever (R17)** — CJK/Cyrillic/Greek/Hebrew are right, Arabic/Devanagari/Thai embed and read WRONG. `fsType` licence disclosure (R109) still owed; applying a donor to EXISTING text still unbuilt (Pass 21.2/21.3) |
| [x] | [x] | [x] | ? | **Delete one text run without deleting every run sharing its text object** (`Pass 32.0`, core+CLI 2026-08-09, GUI same day `03c4c0f`) — a CAD exporter's `BT`…`ET` boundary is a batching artefact, not a per-label boundary; on the operator's own drawing one text object held all 237 dimension labels, so deleting "a label" deleted every one. `plan_delete_text_run` + `EditSession::delete_text_run` + `pdfce-cli text-run-delete --object N --run M`; refuses (`DeleteWouldMoveNextRun`, remedy stated) when the following run's position is `Inherited` from the one being removed (§9.4.2). **GUI**: the Part rung (Object ▸ Part ▸ Point) is now shared between path subpaths and text runs via a `PartKind` dispatcher — reviewed by `pdfce-ui-specialist`; readout is kind-aware (R83: a run has no drag-to-move and no Point rung beneath it, so the path wording is never reused for it) and the §9.4.2 refusal is disclosed BEFORE Delete is pressed, remedy in the same sentence. Same commit fixed a pre-existing `SaveOutcome::Failed` misrouting for subpath delete refusals too (an unrelated defect the specialist's review surfaced) |

### Vector objects (Inkscape-style editing)

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Select vector objects: click, marquee, Alt+click cycle through overlapping hits (Pass 9a, 12.0, 18.5); **additive multi-select** under Shift **or** Ctrl/Cmd, for both click and marquee, and in the Obj tool (`7d3e44c` — Shift worked since Pass 9a, Ctrl was never consulted). **Verified in the running app** (`9328038`: `newsel=1` → `newsel=2` on Ctrl+click) |
| [x] | [x] | [x] | [x] | Move / delete a whole vector object (Pass 25.x, 28.0) — **the delete disclosure is now scale-aware** (`Pass 32.1`, `e85824a`, 2026-08-09): *"1 object deleted"* now names how many PARTS (subpaths/runs) the deleted object(s) held, e.g. *"…They held 237 separate part(s) between them"* — R168's own argument one level deeper, silent when the count is not news (one part each, or uncountable) |
| [x] | [x] | [x] | **[ ]** | Enter an object and select one part (subpath) (Pass 25.0–25.4) |
| [x] | [x] | [x] | **[ ]** | Move / delete a subpath (Pass 28.0 core, Pass 25.2 delete, Pass 36.0 GUI gesture) |
| [x] | [x] | [x] | **[ ]** | Move a node (anchor point), incl. `re` rectangle corners and reused subpath starts (Pass 26.0, 30.0). **Pass 26.3** (`f8bbdd4`) claims grab-without-descent, a hover mark state and live drag preview — **build record OWED**, see `ROADMAP.md` ⚠ FILING GAP #2 |
| [x] | [x] | [x] | **[ ]** | Delete a node (Pass 36.1) |
| [x] | [x] | [x] | **[ ]** | Edit a Bézier handle / control point (Pass 30.1 core+CLI; Pass 26.1 GUI; Pass 26.3 grab/hover/live-preview — build record owed, as above) |
| — | — | [x] | **[ ]** | Level-ladder rung readout (Object/Part/Point) + Escape-to-ascend navigation (Pass 26.0, 36.2) — no clickable breadcrumb yet |
| — | — | [x] | — | Level survival across an edit: an edit/undo/redo truncates your rung one step at a time instead of ejecting you to Page (Pass 26.2). `Acrobat` `—`: Acrobat has no level ladder, so the question is not meaningful. Ladder proven by unit test; the entry gesture is **not** driven end-to-end |
| — | — | [x] | **[ ]** | **Select MULTIPLE nodes** (anchor points) — additive under Shift **or** Ctrl/Cmd, toggle-off on a re-click, every selected anchor drawn as selected (Pass 41.0; discharges part of Pass 23.3). **Restriction CLOSED 2026-08-09 (`6fb7ffb`): the drag gesture now moves the whole selection rigidly** (see *Move a multi-node selection together*, below) — R168's node-rung defect (drag committed one anchor while the canvas painted N as selected) is fixed, matching `Pass 47.0`'s object-rung fix. **Verification limit CORRECTED — the prior claim here was STALE:** the Node rung IS reachable through the scripted harness; the earlier blocker was rapid successive double-clicks coalescing into one burst (documented + fixed, `D:\dev\rag\egui\egui_rapid_successive_double_clicks_coalesce_into_one_burst.md`, ~45 idle frames between gestures). Driven end to end this session: `tool:obj` → double-click → Object → double-click → Part → double-click a corner → Point (`node: Some(0)`) → Ctrl-click a second corner (`set={0, 1}`) → drag → `commit-nodes idx=1 n=2 moves=[(0, (200,80)), (1, (280,80))]`, both corners translated +30 exactly |
| [x] | [x] | [x] | **[ ]** | **Move a multi-node selection together — ONE surgery, ONE undo entry** (`plan_move_nodes` + `EditSession::move_nodes`, core; discharges Pass 23.3's residual "multi-node move" component — `e1430d8` core / `6fb7ffb` GUI / `dbdd9a6` CLI, 2026-08-09). Buckets anchors by the OPERATOR that defines them, not by node, so N corners of one `re` still collapse into ONE §8.5.2.1 Table 59 expansion, and an implicit `h`-reopened start sharing a byte range with its own segment's endpoint is one bucket instead of two overlapping (and silently-dropped) edits. Refuses `EmptyMove` and `DuplicateNodeInMove` before planning any byte. **GUI drag wiring SHIPPED (`6fb7ffb`): `Commit::Nodes` (replacing `Commit::Node`) routes every node drag — single-node grabs included — through `move_nodes`; a grab inside the selection translates the set rigidly, a grab outside moves only itself, a stale selection index is dropped not guessed.** **`cli` SHIPPED (`dbdd9a6`, 2026-08-09): `pdfce-cli nodes-move <input> --object N --move NODE,X,Y [--move …] -o out` — each `--move` is one comma-joined token, deliberately, so three parallel flag lists can't drift out of alignment; every token parses before the file opens, so a malformed fourth `--move` cannot apply the first three. All core refusals map to exit 9, a malformed argument to exit 1.** `Acrobat` **[ ]**: same source as the rest of this section — *"no node/anchor-level model exists in Acrobat at all"* |
| — | — | [x] | [x] | **Modeless select/edit — no tool armed IS the object-edit tool** (Pass 40.0): click selects, double-click descends, drag on the selection moves it, drag on empty paper is still a marquee. Creation verbs (Add Text, Measure) keep explicit arming, because "place new text here" cannot be disambiguated from a marquee by what is under the pointer. **Clicking a TEXT object still does not begin text editing** — operator question (bc) is **RESOLVED**: Edit Text stays a toggle, but the toggles are now independent (Pass 42.0), so arming it costs you nothing else |
| — | — | [x] | ? | **Object tree sidebar — nested object → subpath → node** (Pass 43.0), toggled from View ▸ Objects, dockable and tabbed. Clicking a row **sets the canvas level**: a row's `(object, subpath, node)` triple *is* an `EnteredObject`, so tree and canvas agree by construction. Front-most-first; virtualized, so a collapsed tree costs exactly the object count. **Restrictions: no marked-content / OCG grouping** — deliberately refused, that structure is not decomposed yet (Pass 23.2's core half is unbuilt) and inventing it would misdescribe the document; **scroll-reveal follows a selected OBJECT only**, not a selected subpath or node |

### ce dimensions

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | ◐ | Author a ce dimension with snapping: linear/radius/diameter (Pass 12.M1, 12.M2, 12.M2b) |
| [x] | [x] | [x] | **[ ]** | ce-dimension groups: scale, number format (decimal/fraction), ANSI/ISO drafting standard (Pass 12.M2, 27.2; GUI controls in Properties since Pass 34.2 — **reached from the ribbon's File tab and rendered in the Tool compartment since Pass 43.0**) — group-wide: a change regenerates every member's `/AP` |
| [x] | [x] | [x] | **[ ]** | Change a PLACED circular ce dimension between the radius and diameter reading (Pass 34.2, `EditSession::set_dimension_display` / `pdfce-cli dimension-display`) — **circular only**; a linear target is refused by name (`EditError::NotACircularDimension`). Per-object: regenerates exactly one `/AP` |
| [x] | [x] | [x] | [x] | Drag to reposition a ce dimension (Pass 25.5) — **★ 2026-08-08 finding (decision 033 §1.5): a SEPARATE core verb `EditSession::move_dimension` (`edit.rs:10519`) exists and is called by NEITHER the GUI NOR the CLI** — this row's reposition capability is delivered through `place_dimension`, not through it, despite `main.rs:18498` claiming otherwise. R151 (a shipped capability reachable from no surface is a defect) applies to `move_dimension` itself; not filed as a Pass — no capability gap, a dead-code/doc-comment finding only |
| [x] | [x] | [x] | ? | Edit a placed ce dimension's placement NUMERICALLY — standoff / value-position (Pass 27.1 core+CLI `dimension-offset`; Pass 34.2 GUI spinners, which mirror the drag rather than replace it) |
| [x] | [x] | [x] | [x] | Toggle a ce-dimension group's OCG layer visibility (Properties since Pass 34.2 — **ribbon-activated, rendered in the Tool compartment since Pass 43.0**; the floating Group Manager window is gone, closing R81's last named holdout) |
| [x] | [x] | [x] | [x] | Delete a ce dimension: annotation + `/AP` + `/PieceInfo` sidecar, together (Pass 25.6) |

### Annotations & markup

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Render & count annotations/widget appearances, honouring the annotation-flag set (Pass 6.0) |
| [x] | [x] | [x] | [x] | Author geometric markup: Ink/Square/Circle/Line/Polygon (Pass 6.1) — minimal menu affordance only; no canvas drag/freehand drawing yet |
| [x] | [x] | [x] | [x] | Author text-bearing annotations: FreeText/Stamp with §12.7.3.3 variable-text appearance (Pass 6.2) |
| [x] | [x] | [x] | [x] | **Read an annotation's note text, author and modification date** — `/Contents` (via the §7.9.2 text-string decoder, so UTF-16BE is right), `/T` (**Table 170, markup-only** — `None` means "this subtype has no such concept", never "anonymous"), `/M` (**stored RAW**, because §12.5.2 types it "date *or* text string" and obliges readers to accept any format) (Pass 38.4 core). `cli` `[x]` (**Pass 38.5**, `0a727bb`, 2026-08-09): `list-annotations` gained `author=`/`note=`/`modified=` columns and `with_note=`/`with_author=` summary counts |
| [x] | [x] | [x] | [x] | **Delete an annotation — the general verb, routing (not absorbing) into the three specialised deletion verbs it was carved out of** — `EditSession::delete_annotation`, CLI `delete-annotation --page --index -o --mode --verify-undo` (**Pass 38.5**, `0a727bb`, 2026-08-09). Handles three cascades on OTHER objects the operator did not name: the `/Popup` companion goes with it (§12.5.6.14 — an orphaned pop-up would otherwise start DISPLAYING its own `/Contents` per §12.5.6.2 NOTE 2, i.e. the deleted text reappears); every `/IRT` referrer survives, un-linked on both `/IRT` and `/RT` together (Table 170's `/RT` default is `R`, so removing only `/IRT` would silently reclassify a `/RT /Group` subordinate as a reply); `/AP` streams are removed only when unshared (a producer stamping one form XObject across many annotations is legal under §12.5.5). **Routes, does not absorb:** an unapplied `/Redact` mark → `delete_redaction_mark`; a ce dimension → `delete_dimension` (keeps the STRICT gate — a `/PieceInfo` sidecar rewrite is not on Table 254's `/P 3` list, so **on a `/P 3` certified document a ce dimension's deletion is REFUSED while every other annotation's is ALLOWED** — the standard's own answer, not an inconsistency); a `/Widget` is **refused, not routed**, by name, naming both `delete_widget` and `delete_field` so the caller — not a guess — decides which. **First pdfce verb any `/P` value's permitted list affirmatively names** (§12.8.2.2 Table 254, `/P 3`) — a new `annotation_deletion_refusal()` query is `/P`-aware where the pre-existing `deletion_refusal()` is strict-always. A shared private planner backs both the mutating verb and a pure `annotation_deletion_preview()` query, so a GUI pre-flight tooltip cannot promise a count the delete does not deliver. **Limits: does not remove bytes from the file** (Annex H.7.3 — the default incremental save leaves the deleted object's bytes recoverable; only a full rewrite drops them — deleting a comment is not redacting it); does not chase `/S /Hide` actions or update `/StructParent`/`/OBJR` (named gaps, not oversights). ~~**Pass 6.1/6.2's annotation CREATION/MODIFICATION verbs still take the strict gate even though Table 254's `/P 3` also names them** — an over-refusal this Pass did not fix, see `ROADMAP.md`'s owed item.~~ **FIXED same session, 2026-08-09 (`a4c1a8e`) — `add_markup` (6.1), `add_text_annotation` (6.2), `add_redaction` and `delete_redaction_mark` all now take `check_certification_for_annotation()`, the same `/P`-aware gate this row's own delete verb uses.** `delete_dimension` is UNCHANGED and still takes the strict gate (its `/PieceInfo` sidecar rewrite is not on Table 254's list) — the ce-dimension asymmetry above stands. Verified `p3_permits_authoring_an_annotation_as_well_as_deleting_one` in `annot_deletion.rs`, asserting both directions (`/P 3` permits authoring, `/P 2` still refuses it). |
| — | — | [x] | [x] | **Comments panel — browse every note and markup in the document** (Pass 38.4): subtype · page · author · note text, with a Go-to button per row. Widgets and popups excluded; **ce dimensions ARE listed** (they are `/Line` annotations, and excluding by subtype would also hide a genuine hand-drawn `/Line` markup). When *every* row lacks a note the panel says why, once, at the top — because pdfce's own markup authoring never sets `/Contents`, so a correct list would otherwise read as a broken one. ~~**Restrictions: no Delete** (needs the `delete_annotation` core verb — omitted, not greyed, per R83)~~ **DELETE SHIPPED (Pass 38.5, `0a727bb`, 2026-08-09)** — see the new row above; gated by the master edit switch, the document-wide certification, and the per-annotation refusal, with a pre-flight tooltip and a post-hoc status-bar disclosure. **Restriction still standing: no click-to-select-on-canvas** (needs Pass 22.0) |

### Forms (AcroForm)

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Fill form fields — core/CLI: text, checkbox, radio, choice; **GUI: text + checkbox only** (Pass 7.0, 7.1 core/CLI; Pass 37.2 GUI Forms panel, P0) — radio groups and choice fields are recognised, listed, and disabled-with-a-reason (P1), as are read-only, signature and pushbutton fields. **Rich-text fields are REFUSED in the GUI** (filling would silently discard stored formatting) — core/CLI do NOT yet refuse; see `ROADMAP.md` *Next up* |
| [x] | [x] | [ ] | [x] | Flatten a form to static page content (Pass 7.1) — GUI is Pass 37.2's P1, not built |
| [x] | [x] | [ ] | [x] | Import/export form data, FDF/XFDF (Pass 7.1) — GUI (Batch Tools) is Pass 37.2's P1, not built |
| [x] | [x] | ◐ | [x] | **CREATE a form field — text, check box, **radio**, choice, **push button** — with a name-collision RESOLVER** — `add_text_field` / `add_check_box` / **`add_radio_button`** / `add_choice_field` over `forms_author::resolve_field_path`, CLI `add-text-field` / `add-check-box` / **`add-radio-button`** / `add-choice-field` (Pass 20.1 PARTIAL `8e799e9`; Pass 20.2 + 20.3 PARTIAL `bca60c9`; **Pass 20.0 + Pass 20.1 (completion) `a3d885b`+`f809857`**; **Pass 20.2 COMPLETE `69ab966`+`834d256`+`817b268`**; all 2026-08-07). Field+widget+`/AP`+`/Annots`+`/Fields` land as ONE undo. Check boxes get a keyed `/AP /N` sub-dict with a **vector-drawn** check (R92 forbids a second, symbolic-font generator); choice fields carry `/Opt` export↔display pairs and Combo/Edit/MultiSelect/Sort, and `Sort` **sorts the array**, not just the flag. **Same-name adds now MERGE rather than refuse** — the resolver classifies every add as CREATE (vacant), MERGE (same-type terminal, incl. Shape A→B promotion), or one of two refusals (different-type collision; name held by a grouping node), all four outcomes live across all three verbs. Refuses XFA, any certified document (strict gate — creation is structural), a degenerate rect, an empty name, `Off`/empty as a check box's on-state, `Edit` without `Combo`, a duplicated `/Opt` export. **RADIO GROUPS are built OUT OF the merge primitive, not by a radio-specific path** — three `add-radio-button` calls sharing one name make ONE field with THREE widgets (§12.7.3.2 grouping by shared FQN), and the already-shipped `set_button_state` supplies mutual exclusion untouched. Round widget = pdfce's own stated design choice (the spec distinguishes radio by `/Ff` bit 16 alone), because a group drawn as squares lies about its behaviour. Refuses duplicate export values **unless `RadiosInUnison`**, refuses cross-KIND merge (check box ↔ radio), and refuses **positional-`/Opt` groups BY NAME** (Table 227's index semantics are unresolvable on pdfce's write side — decision 020 §8.3's refusal branch, chosen). Group flags (`NoToggleToOff`/`RadiosInUnison`) are **DISCLOSED, not applied**, since a joining member must not silently rewrite how existing members behave. **Limits: `/MK` borders do NOT paint in pdfce** (R43) — so a check box shows a box and a choice field does not; ~~**no `/I`/`/TI`, no push buttons**~~ **[★★ ALL THREE DISCHARGED 2026-08-08 (`baeb624`, Pass 20.3 COMPLETE) — see the *CREATE a PUSH BUTTON* row below and the `/I`//TI` note in it]**, **no field PROPERTY editing** ~~(rename/reflag/move/resize)~~ **[★ CORRECTED + PARTLY SUPERSEDED 2026-08-07 (`3d345aa`): RENAME NOW EXISTS — see the *RENAME a form field* row below. And that four-item parenthetical was never decision 020's scope: F6 = `--defaults-from` + `rename-field` only; **move, resize and re-flag are UNSCOPED**, not deferred — `ROADMAP.md`'s `Pass 20.6 (PARTIAL)` entry §1 carries the grep that establishes it]** **[★★ SUPERSEDED IN PART 2026-08-07, twenty-ninth filing: **move and resize are now SCOPED as `Pass 46.0`** on the operator's own request (*"form fields and everything else should be draggable and resizeable"*), filed as a **geometry** capability rather than a forms one — see the `Pass 46.0` row under *Planned*. **RE-FLAG alone remains UNSCOPED.**]**. **`/TU` is now MANDATORY-OR-DECLINED** (`TooltipChoice` — R105; `EditError::TooltipDecisionRequired` refuses omitting both `--tooltip <text>` and `--no-tooltip`; declining writes no `/TU` at all, not an empty one) **and the tagged-document + `/Tabs /S` untagged-field disclosures now fire** (decision 020 §3.5.3/§3.4.3, `FieldAuthorDisclosures`/`report_field_disclosures`, all shared across the three verbs) — both closed 2026-08-07 by `50a5461`. **Note: this is the DISCLOSURE only — `/Tabs` tab-order AUTHORING is still not built (Forms P2, below).** **CLI verb shape RULED 2026-08-07: the three flat `add-*` verbs STAND; decision 020's `forms add-field --type …` is SUPERSEDED** (52 flat subcommands in `pdfce-cli`, zero nested — `docs/decisions/020-form-field-authoring.md` §0). No longer an open item. **`gui` is `◐`, not `[x]`, as of Pass 20.5 PARTIAL (`8a8678e`+`165dd49`, 2026-08-07):** all four types can be **placed** on the canvas (`CanvasTool::PlaceField`, new ribbon group beside Forms; click = type-dependent default box, drag = explicit; auto-name **scans and cannot collide**, unlike Acrobat's session counter, because pdfce merges same-name same-type fields; R105 tooltip gated in the pane; merge outcome disclosed only **after** Accept, since the resolver runs inside the core verb). **What the GUI cannot yet set: the per-type detail fields — multiline, initial state, and the choice option editor — all deliberately CUT.** Push button is **absent rather than greyed** (R83/R124) — ~~its verb name is still NOT RULED~~ **[★ 2026-08-08: the verb IS RULED (`add-push-button`) and the type IS creatable, so the recorded reason for the palette omission has EXPIRED. The palette entry is now a cheap un-taken follow-up, not a standing refusal.]** **★ `--defaults-from <field>` (Pass 20.6 COMPLETE, `247b8fa`, 2026-08-07) — all four verbs take a template field. Copies `/MaxLen` (text), `/Opt` export↔display pairs (choice), the on-state from `widgets[0]` (check box), **`/MK /CA` from `widgets[0]` (push button — `baeb624`, so it is FIVE verbs now, not four)**; a RADIO template copies nothing (on-states live per widget, the flag names a field); a non-UTF-8 `/Opt` entry copies nothing rather than being lossily converted. EXCLUDED BY DESIGN: every boolean (presence flags make absent and false one token, so a copy could ADD a property but never turn one off), `/TU` (R105 — a copied tooltip satisfies the mechanism and defeats the purpose), values, `/AA` (F3 authors no action), the radio export value. NET: no property is both shared and copyable, so a MISMATCHED-TYPE template copies NOTHING and says so. CLI only — no GUI.** **★ FUNCTIONAL BUG FOUND 2026-08-08 (decision 033 §1.5): the GUI's `commit_field_draft` (`main.rs:15352`) passes `Vec::new()` for `/Opt` UNCONDITIONALLY when placing a choice field from the canvas palette — every choice field the GUI creates has ZERO options and can never be filled. Core/CLI `add-choice-field` are unaffected (they already require and write real `--option` pairs); this is GUI-only. **FIXED as `Pass 47.4` (`40daa18`, 2026-08-08)** — `commit_field_draft` no longer passes `Vec::new()` for `/Opt` unconditionally. GUI also reaches only `tooltip` of the ~30 authoring properties on all five `New*` specs (decision 033 §1.5) — the rest, plus per-type detail fields generally, remain Pass 20.5's owed remainder.** |
| [x] | [x] | [x] | [x] | **DELETE a form field or a single widget** — `EditSession::delete_field` / `delete_widget`, CLI `delete-field` / `delete-widget` (Pass 20.2 completion, `817b268`, 2026-08-07; decision 020 §3.6.3). Un-lists the widget from its page `/Annots`, drops it from `/Kids`, deletes the object, and prunes grouping nodes left childless (a named node with nothing under it still occupies its §12.7.3.2 FQN slot and would refuse a later field). **The rule that earns its code: deleting the widget whose on-state is the field's `/V` leaves `/V` naming a state no remaining widget can display** — a malformed field that parses perfectly — so `/V` and every surviving kid's `/AS` go to `/Off` **and the operator is TOLD** (`selection_cleared=1` + prose; §3.6.3 says silence either way is the sneaky outcome). Last-member `delete-widget` **delegates to `delete_field`** so the two paths cannot disagree about what *gone* means. **No Shape-B→A collapse on the way down** (R102) — a 3→1 group keeps its `/Kids` parent, because deletion has no business rewriting object identities nobody asked it to change. Two verbs, not one optional `--index`, deliberately. ~~**`gui` stays `[ ]` BY CHOICE, not by oversight:** the Forms-panel deletion surface was scoped into Pass 20.5 and **deliberately cut** — *"half of both is worse than all of one"* — and stays owed under Pass 20.5's own ID.~~ **`gui` IS NOW `[x]` (Pass 20.5 addendum, `fc51786`+`69db1c6`, 2026-08-07):** the Forms panel gives one widget a single "Delete field" and several widgets a row each plus "Delete entire field" — **which verb fires is decided by the row level clicked**, not by a checkbox. **Read-only, signature and push-button rows get the control too** (the panel used to skip them for being unfillable — deletability is a different question; **R83 amended**). Both `FieldDeletion` disclosures are reported (`selection_cleared` and `emptied_parents`). No confirm gate — decision 024 §4.4 — with a tooltip for the one unpredictable consequence (removing the widget holding a radio group's selection). Gated by a NEW `EditSession::deletion_refusal()`, the **strict** certification gate, not `fill_refusal`'s `/P`-aware one: **on a certified fillable form at `/P 2` the fill control is enabled and the delete control is disabled**, and until this commit the GUI had no way to ask. **Still owed under Pass 20.5: the per-type detail fields, and the rendered appearance is not visually verified.** |
| [x] | [x] | [ ] | [x] | **RENAME a form field — and a one-field request reports when it renamed six** — `EditSession::rename_field` returning `FieldRename { descendants_renamed, … }`, CLI `rename-field` (**Pass 20.6 PARTIAL**, `3d345aa`, 2026-08-07; decision 020 §6's F6, half of it). **A subtree rename is ONE object write:** §12.7.3.2 derives the fully-qualified name by descending from `/AcroForm /Fields` and joining each node's `/T`, so renaming `Personal.Address` to `Location` re-derives `Personal.Location.Zip` and `Personal.Location.City` **without writing either descendant's dictionary** (`Personal.Name` untouched; `descendants_renamed=2`). `Field.parent` is what computes the **blast radius**, not what performs the write. **Rule 4 is why the count is returned** — renaming a group silently breaks every FDF, JavaScript reference and submit mapping that named the old FQN. **A rename onto an occupied name REFUSES** (`FormAuthorError::RenameCollision`) — it does **not** merge as a same-type `add-*` does, because the caller named an existing field **and** a new name and merging would destroy an identity never offered up; and it does not auto-suffix, because `Name_2` is a name the operator did not choose. **`FormAuthorError::DottedPartialName` is a SEPARATE refusal from `PeriodInPartialName`**: `A..B` is malformed, `A.B` is a well-formed path that simply is not a *partial* name, and telling the second reader "contains an empty name segment" sends them hunting a typo that is not there. Assertions are on the **saved bytes**, not through `parse_acroform` (R159). **`gui` is `[ ]` BY CHOICE (R151), not oversight** — F5's own per-type detail fields are still owed and a second unreachable core surface would repeat the pattern; what a GUI half needs is a rename affordance on the existing `forms_panel` rows plus `descendants_renamed`, which already crosses the core boundary. ~~**F6's other half, `--defaults-from <field>`, is NOT built.**~~ **[★★ 2026-08-07, `247b8fa`: IT IS BUILT — see the *CREATE* row above. **F6 IS CLOSED and Pass 20.6 is COMPLETE, not PARTIAL.** F6 closing does NOT mean field editing is done: resize is built for nothing, move is built for widgets only (`Pass 46.0` PARTIAL), re-flag is UNSCOPED, and neither F6 item has a GUI.]** |
| [x] | [x] | [ ] | [x] | **CREATE a PUSH BUTTON — the field type defined by what it does NOT have, and the last standard Acrobat type pdfce could not make** — `EditSession::add_push_button` / `NewPushButton`, CLI `add-push-button` (**Pass 20.3 COMPLETE**, `baeb624`, 2026-08-08; decision 020 §6's F3, its unbuilt half). §12.7.4.2.2: a push button *"retains no permanent value"* and *"shall not use the `V` and `DV` entries"*, and **every** structural difference from its two `/FT /Btn` siblings falls out of that one sentence — **no `/V`, no `/DV`, no `/AS`** (absent, not written as `/Off`: `/AS` selects an appearance STATE and there is none), and **`/AP /N` is a plain STREAM**, not the state-keyed sub-dictionary §12.7.4.2.3 gives check boxes and radios. That last is the only place the three button kinds diverge structurally rather than by a flag bit, and it is asserted against the written dictionary rather than the model, because `FieldValue::Absent` is what the reader reports for a `/V` of `/Off` too. **EVERY push button discloses that it is INERT** (`push_button_inert`, `inert=1` on the machine-readable line as well as in prose) — decision 009 posture A means pdfce never authors an action, so this is the only creation verb whose successful result is **a control that does not work**, a caveat true 100% of the time and therefore exactly the kind that decays into a doc comment and then into nothing. **There is no `--required`, and the refusal is BY CONSTRUCTION** — `/Ff` bit 2 means *shall have a value at export*, a push button never has one, so a required push button states a condition no operator action can satisfy; `NewPushButton` simply has no such field, so there is no error variant and no message anyone must read. `read_only` IS offered, because on a button bit 1 has a real reading (inert control, action not yet bound). **The caption is `/MK /CA` and is NOT derived from the name or the tooltip** — `/T` is for scripts, `/TU` for screen readers, `/CA` for the person clicking, three strings for three audiences; an empty caption is allowed and **disclosed** (`push_button_no_caption`), because a blank plate and a forgotten `--caption` are the same bytes. **A merged widget keeps its OWN caption** — `/MK` is a widget key (Table 189), deliberately the opposite of the on-state case where merged check-box widgets must agree: those are views of one exported VALUE, these are views of one ACTION. **Verified by RENDERING at four sizes** (plate, keyline, vertically-centred caption; blank plate; a caption wider than its plate, which clips **left-anchored** so the operator sees the start and can tell). **Limits: no GUI (R151 — the palette shows push button ABSENT rather than greyed, and the recorded reason for that omission has now expired); no action, ever (decision 009); `/MK` borders still do not paint (R43) — the plate is vector artwork inside the `/AP`, with `/MK /BC`+`/BG` written alongside for viewers that synthesise their own, both fed from one public `PUSH_BUTTON_PLATE_GRAY` so the baked and declared looks cannot drift.** |
| [x] | [x] | — | [x] | **`/I` and `/TI` — a choice field's selected-index array and top index, both now spec-correct** (**Pass 20.3 COMPLETE**, `baeb624`, 2026-08-08; §12.7.4.4 Table 231). **`/I` was not missing — it was written, and WRONG twice.** (a) **Unsorted**: Table 231 says *"sorted in ascending order"*, and the indices were collected in the order the caller named the selections, so `Pick=Argentina\|Canada` against `[CA MX AR]` emitted `[2 0]`. Now sorted and deduplicated — while **`/V` is deliberately NOT sorted**, because it is the caller's list of exported values and reordering it would change what the form submits to make an index array tidy. (b) **Written on single-select fields**: Table 231 scopes `/I` to `MultiSelect` and says that when it disagrees with `/V`, *"the `V` entry shall be used"* — a redundant key whose only defined behaviour on conflict is to be ignored. Now removed, **unconditionally**, which also clears a stale one left by another producer. **`/TI` was never written at all** — fill the fortieth option of a fifty-option list and the operator opened the form to options one through six with no visible selection. Now derived on list-box fill from the widget height and the resolved `/DA` size: **absent** while the selection fits the first window (Table 231's default IS 0, so the honest encoding of "top" is no key), else `min(first, count - visible)` — clamped so a selection near the END does not scroll past the last option — **and REMOVED when a later fill lands back at the top**, which is the case an "only write when scrolling" implementation gets wrong. `visible_line_count` lives in `vartext` beside the private `TEXT_PAD`/`LINE_FACTOR` it depends on, so the fold cannot drift from where the generator actually puts lines. **Reported in `FillOutcome::top_index` and in words on `fill-field`** — nothing is guessed, but it is document state pdfce wrote unasked (rule 4), and a scroll position visible only inside a saved dictionary is invisible until a different program renders it. **★ Both defects were invisible in pdfce and visible in Acrobat**, which renders a list box as a live control from `/Opt` regardless of `/AP`, while pdfce's own appearance paints only the selected values — so the project's usual render-and-look verification is structurally incapable of catching anything here. `gui` is `—`: these are fill-path byte semantics with no operator-facing control of their own. |
| [x] | [x] | — | [x] | **READ a widget's caption (`/MK /CA`)** — `forms::Widget::caption`, surfaced as a trailing `caption=` column on `list-fields` (`baeb624`, 2026-08-08). **One key out of Table 189's eleven, and the count is the point**: the other ten are cosmetic and R43 means pdfce never synthesises appearance from `/MK`, so modelling them would add read surface nothing consumes. `/CA` is different because **on a push button it is not cosmetic** — with no `/V` in any state it is the button's only human-readable identity, the only thing telling *Submit* from *Reset* in a field list, and the only property `--defaults-from` can copy from a push-button template. Read on **every** widget rather than only on buttons (`/MK` is legal on any widget annotation, and a type-gated reader would make the model silently disagree with the file), and appended **last** on the `list-fields` line so a parser reading through `aa=` is unaffected. |
| [x] | [x] | [ ] | [x] | **MOVE a form-field widget — and the placement algorithm carries the artwork, so nothing is regenerated** — `EditSession::move_widget` returning `WidgetMove { siblings_left_behind, … }`, CLI `move-widget` (**Pass 46.0 PARTIAL**, `fd6eadd`, 2026-08-07; first slice of the operator's *"form fields and everything else should be draggable and resizeable"*). **A move needs NO `/AP` regeneration and that is a fact about §12.5.5, not a shortcut:** step (b) makes the per-axis scale `Rect_extent / box_extent`, so an unchanged extent gives 1 and 1 and matrix **A** degenerates to a **pure translation** — every conforming reader carries the existing artwork, unscaled, for free. **One object is written**; verified `[20 150 160 172]` → `[60 150 200 172]` with the three original `/Rect` arrays untouched in the base revision. **A widget is not a field** — a field may own widgets on several pages, so this moves ONE and discloses `siblings_left_behind` in prose (*"moved widget 0 only — 2 other widget(s) stayed where they were"*), because moving one of three silently reads as a bug an hour later. **A `/Rect` that is absent or malformed is REFUSED, not fabricated** (§12.5.2 requires it; inventing an origin puts the widget where the file never said). Byte-asserted on the saved file (R159) — and the assertion had to match `"45.0 140.0"`, not `"45 140"`, because `serialize.rs` always writes a decimal point so a re-parse yields `Real` and not `Integer`. **LIMITS, and they are the bulk of `Pass 46.0`: RESIZE IS BUILT FOR NOTHING**; **only widgets move** — markup annotations, redaction marks and links have no move verb (ce dimensions have their own, `move_dimension`, Pass 25.5); **no GUI** (R151 — a second core geometry verb with no shell caller). **Resize is REACHABLE, not blocked:** all four widget generators are already size-parameterised and position-independent, but **markup appearances hold ABSOLUTE geometry** (`/BBox == /Rect`), so resizing one means rescaling its `MarkupSpec` — two jobs of very different size |

### Redaction & security

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Mark redactions by text search or named region (Pass 8.0, 8.1) |
| [x] | [x] | [x] | [x] | Mark redactions by PATTERN — `#` = any digit, `?` = any character, so `###-##-####` marks every SSN-shaped run in one action (Pass 8 core/CLI `redact-mark --pattern`; Pass 37.0 GUI `Match: Exact text \| Pattern` switch over the existing query box) |
| [x] | [x] | [x] | ◐ | Apply redaction with a runtime-verified true-removal proof (Pass 8.0, 8.1) — Acrobat applies, but gives the operator no automated proof it removed what it claims to |

### Fonts & rendering

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | — | [x] | [x] | Rasterize page content: vector paths, text, images (Pass 1, 1.1). **Large CAD drawings are SLOW but no longer pathological** (`76200e9`, 2026-08-07): a 129,515-path ArchiCAD sheet went 1× 32,313 → **18,870 ms** (−42%), 2× 447,862 → 214,714 ms (−52%), **output byte-identical**. **Painting every path is 0.87 s of that — the clip machinery was 95% of render time**, and the fix removed a page-sized `clip.clone()` per paint (~108 GB of memcpy for one page) and bounded `intersect_clip`'s multiply to the path's own device bounds (an identity, not an approximation). **Parse is not the cost at any scale** — read + parse + page tree ≈ 5 ms, ~0.005%. ~~**Still ~18 s, not interactive** — see *Planned*~~ **★ IMPROVED AGAIN (`4475fe6`, 2026-08-07): `GraphicsState.clip` became `Arc<Mask>` — a clip is never mutated in place, so `q` needs a reference, not a buffer. `q`/`Q` clone cost 6.80 s → 0.01 s; 1× 17.47 → **10.18 s**, 2× 214.71 → **51.52 s**. From the original baseline that is **~3× at 1× and ~8.7× at 2×**. **Byte-identical on the CAD sheet AND 52 synthetic fixtures** (JPX, bilevel, annotations, text, vector, CMYK — the CAD page has zero images and 242 text elements, so it cannot carry a no-pixel-changes claim alone). **`Arc` not `Rc` so `GraphicsState` stays `Send`** for future off-thread rendering. ~~**Still ~10 s, not interactive** — see *Planned*.~~ **★ BUT IT NO LONGER FREEZES THE WINDOW** (Pass 44.0, `7926a78`): the ~10 s is spent on a worker thread and is cancellable — see *Off-thread, cancellable rendering* above. ~~**Slow and interruptible, not slow and dead**~~ **★★★ AND IT IS NO LONGER SLOW (`Pass 45.0`, `ce57ed5`, 2026-08-07): the clip-mask cache took 1× to 907 ms end to end (0.79 s render-only) and 2× to 1,425 ms — 35.6× and 314× from the 32,313 / 447,862 ms morning baseline, output BYTE-IDENTICAL (same SHA-256 `9250a89f…`, plus 115 synthetic fixtures). THE RENDER IS NOW FLOOR-BOUND at 0.49–0.53 s and the maximum further speedup at 1× is 1.55×.** See *Interactive-speed rendering of a large CAD drawing*, above, which moved out of *Planned* on this commit — **the "still ~10 s" sentence struck above was true at `4475fe6` and is left visible rather than deleted** |
| [x] | — | [x] | ? | **Off-thread, cancellable rendering — a slow page no longer freezes the window** (**Pass 44.0**, `e4256f2` layers 1–2 + `7926a78` layer 3, 2026-08-07). `RenderCancel` is an `Arc<AtomicBool>` in `pdfce-render` — no windowing, no runtime, no executor, so §3 holds and it works unchanged under wasm. **Measured: pre-cancelled 322 ms against 10,367 ms uncancelled; 28.9 ms from `cancel()` to thread exit mid-render — 58,000 / 28.9 = 2,007×, which is why cancel-then-mutate beat blocking.** `RenderOptions.cancel` defaults to `None`, so **every existing caller keeps an uninterruptible render** and the CLI, the round-trip oracle and the R85 harness cannot acquire a new failure mode from the field existing. Enabled by `ObjectGraph` gaining **`Send + Sync`** — a `pdfce-core` **public API change**, taken now because with no remote and no release the affected implementor set is empty exactly once. **★ NOT A SPEEDUP: a 10 s page still takes 10 s** — it is spent on a thread nobody is waiting on, so the window repaints and the render can be abandoned. **★★ `gui` WENT `[ ]` → `[x]` AT `7926a78`**: `render_worker.rs` (503 lines) supplies the worker, channel, cancellation token and generation counter. **A bounded 12 ms in-frame wait (72% of one 16.7 ms frame at 60 Hz) returns fast pages inline**, so nothing regresses when fast; **an edit mid-render cancels, joins, then mutates at one choke point (`session_mut`), making `Arc::get_mut` infallible by construction**. **A livelock was found by REASONING before it could be observed** — a per-frame staleness check would have cancelled and restarted the same render every frame, so any page slower than one frame would never have finished; `RenderKey` prevents it. **Staleness disclosure is split by case**: a zoom already discloses itself (LINEAR filtering reads as *soft*), an edit does not (sharp and simply wrong), so the **status bar** says the canvas is behind, gated at **150 ms** (= `ZOOM_SETTLE` = 9 frames at 60 Hz), fixed-position per decision 024 §4.4. **⚠ The rendered behaviour is UNVERIFIED BY SCREENSHOT** (`gui-shot.ps1` takes the foreground and the operator was at the machine) — owed. `cli` is `—`: a one-shot batch render has no interactive gesture to cancel it. `Acrobat` is `?` — not sourced from the feature RAG, and not ticked on the strength of an assumption. **★★ THE WORKER IS NOW TRACED (`9681112`, 2026-08-07, no Pass ID — instrumentation, +23 / −0): `render-inline` / `render-async-started` (carrying `budget_ms=12`) / `render-async-done` (elapsed + `done`/`cancelled`/`failed`). Until this commit the render path emitted NO diagnostic trace at all — the module built to make a slow render OBSERVABLE was mute about whether it went off-thread, how long it took, or whether it was cancelled, and its silence had been read as evidence the render worked. Third such channel in one day (`edit_note`, the stale-binary trace, this).** **★ IT PRODUCED THE FIRST IN-GUI MEASUREMENT: `render-async-done gen=1 ms=907 outcome=done`, UI thread processing frames throughout — the same 907 ms the CLI gave, from the instrument the operator actually uses. ⚠ THE OWED SCREENSHOT VERIFICATION IS UNTOUCHED: a trace proves the worker ran and finished, not that the pixels are right.** |
| [x] | — | — | ? | **Interactive-speed rendering of a large CAD drawing — DELIVERED 2026-08-07 at `ce57ed5` (`Pass 45.0`): 1× is 907 ms end to end (0.79 s render-only), INSIDE the reference product's ~1.6 s, and the render is now FLOOR-BOUND at 0.49–0.53 s.** ~~1× is **10.18 s** at `4475fe6`~~ (was 18,870 ms at `76200e9`, 32,313 ms originally); the reference product is *"feels instant"* (uninstrumented; ~1.6 s cold-to-sharp on an M4 Pro). **No Pass ID — deliberately, and this row is why: a number minted a commit ago would now point at a dead end.** ~~(1) stop allocating a page-sized mask per clip — `Mask::new` is **10.1 s of the remaining 18 s**, and most clips are `re W n` rectangles needing no mask at all~~ **★ BOTH HALVES FALSIFIED 2026-08-07: `Mask::new` is 1.02 s, not 10.1 s (an R164 instance — the figure came from an ablation that measured construction *plus* use), and only 612 of 24,128 clips — 2.5% — are rectangles. The rectangle special-case was DECLINED ON MEASUREMENT, not built.** ~~Revised order: **(1′)** size the mask to the clip, not the page — clips are **100% single-subpath, mean 7 segments, mean bbox 0.663% of the page**, so the mismatch is one of *extent*, not *shape*~~ **★★ 1′ IS RETIRED 2026-08-07 (`6b33789`) — THE PREMISE WAS WRONG BY 100×: mean clip bbox is 66.36% of the page, not 0.663%** (a fraction printed as a percent; first clips measure 87/65/100/81/95%). **THREE INDEPENDENT REFUTATIONS**: *size* (a mask sized to a 66%-of-page clip **is** page-sized); *API* (tiny-skia's `RasterPipelineBlitter::new` returns `None` on a mask/pixmap size mismatch — a `log::warn!` and a **silently dropped paint**, so it would have stopped painting, not sped up); *cost* (`Mask::fill_path` is 10.3 µs on a 64×64 mask vs 8.3 µs page-sized — dominated by three raster-pipeline compilations, and `scan::path_aa` already bounds to `path.bounds()`). **The clip-representation line of attack is CLOSED.** Live order is now **(2′)** the cache cliff — **measured and partly discharged**, 1×→2× was 14.1×, now **5.1×**, still above the ~3.2× a pixel-quadrupling costs elsewhere — then **(3)** tiling/threading **last, unchanged**: painting every path is only **0.87 s**, so they would today optimise 5%. `cli`/`gui` `—`: the work is entirely in `pdfce-render`, so **both shells get it with no change of their own** — not "no benefit". **`6b33789` changed NO timings — 1× is still ~10 s; it bought a correction and `tools/render-profile`, the standing instrument that makes a second measurement cheap. No fork is live as of that commit (operator-stated)** **★ THIS ROW IS NOW GOVERNED BY `R166` (minted 2026-08-07): a number whose instrument no longer exists may be reported but may NOT scope, order or build anything — so the live order below (2′) then (3) may only be re-ranked on figures `tools/render-profile` can reproduce.** **★★ THE FLOOR IS MEASURED 2026-08-07 (`fa17d54`, `--ablate-sweep`): 0.49–0.53 s while pixels vary 64× — SCALE-FLAT, PER-OPERATION (148,517 operators). Complete map at 1×: interpreter floor 0.5 s · painting ~0.8 s · mask sampling FREE at the noise floor · CLIP CONSTRUCTION ~8.4 s = 86%, which reproduces the earlier per-phase sum (5.24+2.26+1.02 = 8.52 s) within 4% BY A SECOND METHOD — so this row's ordering is now `R166`-clean. (3) is REFUTED as an answer, not merely last: tiles render fewer PIXELS, not fewer OPERATORS. And at 0.25× the FULL render is 2.57 s, not 0.67 s, so A LOW-RESOLUTION PROXY IS BOUNDED BELOW BY ~2.6 s — proxies and progressive refinement help LESS than pixel count suggests, and clips bind either way. The non-clip work sums to ~1.3 s, inside the reference's ~1.6 s — but that is ARITHMETIC OVER SEPARATELY MEASURED PARTS, NOT A MEASUREMENT (R164 applies); nobody has rendered this file in 1.3 s.** **★★★ THE 86% IS BROKEN DOWN 2026-08-07 (`110b8c9`, TIMED not ablated): `Mask::new` 1.03 s (11.8%) · `fill_path` 5.22 s (216 µs/clip, 59.9%) · multiply 2.46 s (28.3%) = 8.72 s; sum + floor = 9.26 s against a 9.49 s render — THE ARITHMETIC CLOSES. TWO FIGURES CORRECTED: `fill_path` was filed at 8–10 µs/call and is 216 µs (22× — the original experiment varied the BUFFER while the cost is set by the PATH'S EDGES; the conclusion it supported, that buffer size does not matter, is STILL TRUE, so item 1′ stays retired), and PAINTING IS ~0.27 s, NOT 0.87/0.8 s (those were floor + painting — R164's third instance in one day), so (3) tiling/threading addresses UNDER 3%, not 5%. ★ THE PER-CLIP DISTRIBUTION IS UNIFORM — 85.0% in 256–512 µs, p90 and p99 both <1024 µs, only 108 of 24,128 over 1 ms and only 36 under 256 µs. NO TAIL AND NO HEAD, so THERE IS NO SPECIAL CASE TO FIND AND ANY FIX MUST CHANGE THE WORK FOR ALL 24,128 CLIPS. ★ `fill_path` GROWS 2× PER 4× PIXELS — it tracks the LINEAR dimension (the scanline converter follows the path's PERIMETER), and at 0.25× it is STILL 56% of the whole render: the MEASURED reason culling and proxies underdeliver. LIVE CANDIDATE: dedup/cache built clip masks — ~~BLOCKED, deliberately, on censusing how many of the 24,128 are re-applications of an already-built clip path BEFORE anything is built (R166 applied prospectively)~~ **★★ THE CENSUS IS RUN AND THE BLOCK IS DISCHARGED 2026-08-07 (`1992d13`) — AND THE PREMISE SURVIVED, THE FIRST OF THREE THAT HAS: 24,128 applications over 40 DISTINCT paths = 603.20 per path, 24,088 repeats = 99.83%. BUT THE MEAN HIDES THE SHAPE — top-1 = 97.3%, top-2 = 99.8%, and 37 of the 40 are applied EXACTLY ONCE, so A 2-ENTRY CACHE SERVES 99.8% OVER ~1.9 MiB, not a 40-entry 38.3 MiB one (38.3 ÷ 40 = 0.958 MiB per mask). ★★ AND A HIT IS WORTH THE WHOLE OPERATION: a second key including the INCOMING clip returns 40 distinct (path, incoming clip) pairs — IDENTICAL to the 40 build keys — so every re-application is under the SAME incoming clip, the FINAL mask is identical, and a hit can SHARE THE EXISTING `Arc`: 361 µs/clip (8.72 s ÷ 24,128), not the 259 µs (6.25 s ÷ 24,128 = 72%) a build-only cache would save. `q`/`Q` does NOT already solve it — restore is free since `4475fe6`, but every `W`/`W*` calls `intersect_clip` regardless. BOTH IDENTITY CHOICES UNDERSTATE REPETITION BY CONSTRUCTION (bit-exact coordinates; `Arc`-pointer incoming identity), so 99.83% and 40 are LOWER BOUNDS. ⚠ THE ~10 s → ~1.7 s PROJECTION IS ARITHMETIC OVER SEPARATELY MEASURED PARTS (99.83% × 345.6 µs mean = 8.34 s over 24,128 removed, against 1× totals of 9.28–10.18 s), ONE INSTRUMENT EACH, AND IS EXPLICITLY UNVERIFIED — no cache exists, nothing has been re-rendered, and it may NOT re-rank this row on its own (R166). It is the SECOND unverified projection in this row, beside "non-clip work sums to ~1.3 s". `1992d13` CHANGED NO TIMINGS — it is instrumentation only (3 files, +374/−0, of which 12 lines touch the rasterizer), so this row's 10.18 s stands. An engineering fork is building the cache; nothing here describes that work (R87).** **★★ A SECOND, INDEPENDENT LINE OF ATTACK OPENED 2026-08-07 (`e4256f2`) AND IS NOT A SPEEDUP: a render can now be CANCELLED — pre-cancelled 322 ms against 10,367 ms, and 28.9 ms from `cancel()` to thread exit mid-render. This does not make the page faster by one millisecond; it makes the wait INTERRUPTIBLE, which is the difference between an application that is slow and one that is DEAD. ~~THE OPERATOR'S REPORT — *"it took minutes to try and update the view and hung the entire gui"* — IS NOT RESOLVED: layers 1 and 2 shipped, LAYER 3 (`Arc<EditSession>`, worker, channel, generation counter, all in `pdfce-gui`) WAS DELIBERATELY CUT, AND THE GUI STILL FREEZES.~~ **★★ RESOLVED 2026-08-07 (`7926a78`, and the whole thing is now `Pass 44.0`): LAYER 3 SHIPPED AND THE FREEZE IS GONE — but this row is UNCHANGED, because not one millisecond came off the ~10 s.** The two lines are independent: cancellation and off-thread make ~10 s **survivable**, and only the clip work makes it **short**; **no figure below may be re-ranked on the strength of Pass 44.0** (`R166` still governs). What Pass 44.0 *does* change for this row is that the shell can now show an out-of-date picture while a better one computes — the precondition a proxy or progressive-refinement scheme needs — while **the measured ~2.6 s floor under a 0.25× proxy still refutes the naive version**. ALSO RULED, with a number: on an edit arriving mid-render — cancel, wait, mutate, one choke point, 28.9 ms against ~58 s for blocking (**2,007×**).** **★★★ DELIVERED 2026-08-07 (`ce57ed5`, **`Pass 45.0`**) — THE CLIP MASK CACHE LANDED AND THIS ROW'S TARGET IS MET ON THE REFERENCE SHEET. Measured by the ENGINEER end to end (incl. process start + PNG encode): 1× **32,313 ms → 907 ms (35.6×)**, 2× **447,862 ms → 1,425 ms (314×)**. Measured by the fork's `render-profile` harness (render phase only): 1× **10.68 s → 0.79 s (13.5×)**, 2× **58.52 s → 1.30 s (45.0×)**. **BOTH PAIRS ARE FILED AND NEITHER IS RECONCILED AWAY** — 35.6× is DAY-CUMULATIVE over three fixes, 13.5× is THIS COMMIT ALONE, and the after-figures differ by a near-constant 117/125 ms of process start. The reference product's *"feels instant"* is ~1.6 s cold-to-sharp (uninstrumented), so **pdfce is now inside it at 0.79–0.91 s.** **★★ THE RENDER IS FLOOR-BOUND: 0.79 s against a measured interpreter floor of 0.49–0.53 s (scale-flat over a 64× pixel span, 148,517 operators = 3.43 µs each), so the floor is 62–67% of what is left and THE MAXIMUM FURTHER SPEEDUP AT 1× IS 1.55×.** Clip construction is now **41 rebuilt masks × 362 µs = 14.8 ms = 1.9% of the render**, down from **8.72 s over 24,128 applications = 361 µs each = 86%**; hit rate **24,087 + 41 = 24,128 = 99.83%, EXACTLY the census ceiling**, the 41st build being the single eviction 4 slots make over 40 distinct paths. **★ THE ~1.7 s PROJECTION WAS BEATEN BY 2.2×, AND THE MEASUREMENT ADJUDICATED BETWEEN A FIGURE AND ITS OWN CORRECTION**: floor 0.51 + painting **0.27** = 0.78 s against 0.79 s measured (1.3% apart, CONFIRMED), while floor 0.51 + painting **0.87** = 1.38 s (75% high, REFUTED) — the `R164` painting correction, made on reasoning alone and never independently measured, is now confirmed. **Output BYTE-IDENTICAL: SHA-256 `9250a89f…` on the 1× CAD render, THE SAME HASH AS THE 32.3 s RENDER THIS MORNING, plus an unchanged aggregate over 115 synthetic fixtures.** **`core` goes `[ ]` → `[x]`; `cli`/`gui` stay `—` because the work is entirely inside `pdfce-render` and both shells get it with no change of their own.** ⚠ **MEASURED ON ONE DOCUMENT.** The 99.83% repeat rate is a property of this CAD sheet, not of PDF — a file clipping one path under different accumulated clips gets fewer hits (it cannot get WRONG hits; the incoming clip is in the key). **Whether the shape generalises is NOT measured and is NOT claimed.** Full record: the `ce57ed5` entry at the top of `ROADMAP.md`'s *Shipped*.** **★★ SCOPE WIDENED 2026-08-07 BY ENGINEER RULING: `Pass 45.0` covers the WHOLE CLIP ARC — `76200e9` + `4475fe6` + `ce57ed5` — per-commit 1.71× · 1.72× · 13.52×, the ID's own cumulative figure 32,313 ms → 907 ms = 35.63×. `4475fe6` is what made the cache expressible (a hit hands back a SHARED mask, which needs a shareable clip). NOT widened to `1992d13`/`fa17d54` — instrumentation. The widening minted no number.** **★ AND THE 907 ms IS NOW CORROBORATED IN THE GUI (`9681112`): `render-async-done gen=1 ms=907 outcome=done`, UI thread live — two instruments, same figure, and the second is the one the operator uses.** **★ TWO SEPARATE RENDER-CORRECTNESS BUGS FIXED 2026-08-09** (benign-bucket audit, no Pass ID — see `ROADMAP.md` Shipped; reconstructed from commit messages, not independently re-verified by this filing): non-embedded standard-14-alias text lacking `/Widths` advanced by ZERO, stacking every glyph on one point (`1edf4e3`); sub-pixel stroke widths, including `0 w`, rendered at ~9% contrast instead of §8.4.3.2's device-pixel floor (`9abf5b5`). Both silent since Pass 1/1.1; found by structurally auditing EXPLAINED render-parity divergences instead of only large ones. **★ A THIRD RENDER-CORRECTNESS BUG FIXED THE SAME SESSION (`365856f`, 2026-08-08, no Pass ID — reconstructed from the commit message, not independently re-verified by this filing): `gs` (ExtGState) was a silent no-op whenever a page referenced `/ExtGState` indirectly (the overwhelmingly common form, `/ExtGState << /GS0 12 0 R >>`) — no line width, cap, join, dash or constant alpha applied, and no diagnostic. The affected page had counted `clean=1` in the render-parity harness, helping define the very "benign" threshold it should have failed. Every other resource lookup in the same file (`/Font`, `/XObject`) already resolved indirect references; `gs` did not.** |
| [x] | — | [x] | [x] | Image codecs: DCT/JPEG, LZW, RunLength, CCITT Fax, JBIG2, JPEG 2000 (Pass 2.1–2.3) |
| [x] | — | [x] | ? | **Calibrated `DeviceCMYK`→sRGB conversion — one conversion site for the whole document, not two** (`Pass 49.0`, `edf7c02`, 2026-08-08). Closes the naive-additive gap named since Pass 11: max per-channel error `[11,37,30]`→`[3,2,2]`, pixels differing >8/255 37.40%→0.00%, measured on decision 006's own fixture/method; corpus-paired 18 improved / 63 unchanged / 0 worse over 81 `/DeviceCMYK`-bearing files. 6×6×6×6 grid (1,296 measured sRGB nodes, 15.5 KB), no new dependency, no ICC profile shipped or read. **Consolidates two independently-naive `Rgb::from_cmyk` implementations** (`pdfce-render` and `pdfce-core::vector::geometry`) into one `pdfce-core::color` site both now share — the naive twin in the object model would otherwise have kept reporting a colour the canvas no longer paints. **Target is agreement with a documented default, not colorimetric correctness — Acrobat has no spec-mandated answer here either** (§8.6.4.4 is silent; Acrobat itself resolves via a user-configurable ICC working space). ★ **Operator-visible consequence: solid `0 0 0 1 k` black now renders `#231F20`, not `#000000`** — lands on CAD line drawings stroked in pure K, the motivating case for this project. `cli` `—`: automatic pipeline behaviour, no batch shape of its own. **★ THE CONSEQUENCE IS NOW A SETTING (`Pass 51.0`, `2a1b0df`, 2026-08-08): `cmyk_intent` (`calibrated`/`neutral_black`/`naive`) in `userdata/settings.txt`, threaded core→render→cli→gui per R83 (bundled since `Pass 51.3` in `pdfce_render::font::RenderPolicy`). `neutral_black` renders pure-K ink `#000000` while leaving every mixed colour on the calibrated table** — see the settings-persistence row (Shell & UX) for the store itself. **★★ SHIPPED DEFAULT FLIPPED `calibrated`→`neutral_black` (`Pass 51.2`, `780b2fb`, 2026-08-08, operator ruling "flip it") — the sentence above claiming `#231F20` describes `calibrated`, no longer the default.** Solid `0 0 0 1 k` now renders `#000000` OUT OF THE BOX again; `#231F20` is `calibrated`, an explicit non-default choice. Filed as a KNOWING DIVERGENCE from R169's own best-evidenced-guess framing (`calibrated` remains tier (a)/(c)), not a correction — a settings window (`Pass 51.4`) discloses it as such |
| [x] | [x] | [x] | [x] | **Insert an image into a page as a new XObject (`Pass 48.0`, `ec6556f`; GUI drag-and-drop `Pass 48.3`, `e6ad48c`, both 2026-08-08)** — operator request: *"drag and drop images of different formats onto the pdf workspace."* Core `EditSession` verb + CLI + a canvas drop gesture (natural size from the image's own DPI, centred on the pointer, clamped to the page, sized from DISPLAYED/post-rotation dimensions, page-centre fallback on a miss) all reach it. **★ EXCEEDS BOTH PARITY REFERENCES BY DESIGN** — neither Acrobat nor PDF-XChange places a dropped image inline (PDF-XChange opens it as a separate document); recorded as a deliberate exceed, not a gap to "correct." `Pass 48.3`'s own commit (`e6ad48c`) is subjected `Pass 47.7` in error — see `ROADMAP.md`'s `Pass 48.3` Shipped entry for the Pass-ID collision correction. ~~TIFF scoped OUT by name~~ **★ BUILT (`Pass 48.4`, `6d63d81`, 2026-08-08) — hand-written, no new dependency; `SUPPORTED_FORMATS` is now "PNG, JPEG, BMP and TIFF"** |
| [x] | — | [x] | ? | **Transparency rendering — `/SMask` + `/Mask` (soft/explicit/colour-key), `/Matte` (`Pass 48.1`, `a9a0780`, 2026-08-08); constant alpha `/ca`/`/CA` (`d3ea5de`, 2026-08-09, no Pass ID — see `ROADMAP.md` Shipped)** — closes a gap named since Pass 1.1 ("recognized today, base image drawn opaque with a diagnostic note"). ~~**Limit: `/ca`/`/CA` constant alpha is NOT modelled in the graphics state at all**~~ **★ FIXED 2026-08-09 (`d3ea5de`) — both keys are now read off the ExtGState, applied at paint time, and saved/restored by `q`/`Q` via `GraphicsState` (§8.4.2). Filed under `ROADMAP.md`'s benign-bucket-audit render fixes, reconstructed from the commit message, not independently re-verified by this filing.** Two further named-but-unenumerated cases remain, tracked on the retired `transparency_not_previewed` diagnostic |
| [x] | [x] | — | — | **JPEG write path — `jpeg-encoder` 0.7.1, `Jpeg{quality}` config (`Pass 48.2`, `889566f`, 2026-08-08)** — pdfce's first image ENCODER (R28's first exception; decision record 034 OWED, not yet authored). Write-side CMYK/YCCK polarity: feeds the complement of true-ink samples so the crate's own internal inversion composes into Adobe TN #5116 §13.1's forward transform — no `/Decode` written, transform byte 2. `gui` `—`: `Pass 48.3` (drag-and-drop placement, SHIPPED) did not add a quality-control surface — the drop gesture uses the encoder's default; still no operator-facing quality control anywhere |
| [x] | [x] | [x] | — | Glyph-coverage gate on the app's own UI chrome — no tofu glyphs in pdfce's own strings (Pass 18.7) |

### Shell & UX

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| — | [x] | — | **[ ]** | A first-class scriptable CLI (`pdfce-cli`) over the capabilities above — Acrobat has no CLI at all (Action Wizard, embedded JS and COM only) |
| [x] | [x] | [x] | — | Live-edit canvas: renders the edited revision, not a static page image (Pass 17.x) |
| — | — | [x] | — | Interactive canvas: pan, zoom-to-cursor, marquee select (Pass 12.0, 18.8). **Marquee is currently full-enclosure only** — direction-sensitive selection (left-to-right encloses, right-to-left crosses, rectangle appearance changes mid-drag) is **(be) RESOLVED 2026-08-08, accepted, but UNSCOPED — no Pass ID yet** |
| — | — | [x] | — | Dockable panel shell — **the left dock is Pages + Tool, no tab bar** (Pass 18.1, 34.1, 34.2, 38.2, 38.4, **43.0**). **The ribbon owns the surfaces and the Tool compartment shows them**: Properties (File), Forms (Edit), Comments (Review), Batch Tools + Redact (Tools), and the measure options, each rendering in Tool when activated, with a named "Back to tools" exit. Properties still holds the same three scope-named sections (selected ce dimension · ce-dimension groups · document `/Info`); no persistent floating window remains (R81). **Pass 43.0 partially reversed 38.2's four-compartment layout** — R157 (*watched state gets a compartment, entered workflows share one*) survives; **Properties was on the wrong side of it** |
| — | — | [x] | ? | **Independent edit toggles + one master edit switch** (Pass 42.0) — Edit Text, Add Text, Obj and the measure tools can all be **on at once**; a click is resolved by a documented, **enable-order-independent** precedence ladder (TextEdit → AddText → measure → VectorEdit), and the Tool pane **says which tool has the canvas**. One switch turns **all** editing off, covering the ce-dimension drag, form filling and redaction marking too — each disabled-and-explained, so the document still reads. Turning editing off **keeps the tool set**. **Restriction: the three measure tools remain mutually exclusive AMONG THEMSELVES** (they share one state struct, so two-on is a state with no meaning) — they are fully independent of the text and object tools |
| — | — | [x] | — | **Dock density convention** — one row-spacing constant applied at the single chokepoint every pane renders through (Pass 38.1, `UI_PREFERENCES.md` §11). Measured: 285 label rows before and after, 11 px reclaimed — same ink, shorter panel. **Three refusals are written into the convention** so a later pass cannot spend them: click targets never shrink (`interact_size.y` stays 18.0 — density comes from the gap, not the control), no text gets smaller, and no explanatory line is deleted to save a row (those lines are the screen-reader surface) |
| — | — | [x] | — | Ribbon command surface — File/Edit/Review/Measure/Tools/View tabs, reset-layout chooser covering both docks (Pass 24.1, chain `6449859`→`2b12efe`); **legible as a ribbon since `Pass 47.2` (`88f5097`, 2026-08-08)** — two-row group bodies, centred captions, vertical separators, mixed large/small button forms, fixing three groups that previously rendered with no caption at all — plus a **collapse toggle** (density, per operator-resolved (bf)) |
| — | — | [x] | — | **Quick Access Toolbar — fixed chrome, not tab-gated** (`Pass 47.1`, `9d7e7a0`, 2026-08-08) — Open, Save a copy…, Undo, Redo; **page navigation and zoom moved to the status bar's bottom-right**, per the operator's RESOLVED answer to (bd) — diverges from decision 024 §3.5(d)'s original QAT list, recorded rather than silently absorbed |
| — | — | [x] | ? | **Multi-target verbs act on the whole selection or refuse, never a silent subset (R168, `Pass 47.0`, `5f9e68c`, 2026-08-08)** — `delete_selected_object` and the drag handler no longer read `.iter().next()`; a 5-object Delete removes five or refuses with a stated reason. Prerequisite the right-click context menu bucket (`Pass 47.5`, *Planned*) depends on |
| — | — | [x] | — | **Editor chrome for form-field widgets (R167, `Pass 47.3`, `33b86f5`, 2026-08-08)** — a dashed, tinted outline + type glyph drawn by the editor for every widget when a form context is active, never written to any `/AP`. Makes the Forms panel's rows locatable on the page; prerequisite for `Pass 47.6`'s resize handles |
| — | — | [x] | — | Implicit gesture-commit: clicking away accepts an in-progress edit instead of requiring a separate Accept/Reject click (Pass 34.0) |
| — | — | [x] | — | **Nothing floats over the canvas** — every commit control, refusal and disclosure (Edit Text · Add Text · Measure) lives in the tool's dock compartment; `canvas::tool_strip_anchor`/`StripCorner` deleted (Pass 34.1 slice 4, `7f850c9`). ⚠ **Shipped but NOT yet filed in `ROADMAP.md`** — see the *FILING GAP #2* flag at the head of *Shipped* |
| [x] | [x] | [x] | — | ScripTree-style SVG icon set for every GUI feature (Pass 18.3) |
| [x] | [x] | [x] | ? | **Persisted user settings — the R15 user-state partition, first thing in pdfce that survives a restart (`Pass 51.0`, `2a1b0df`, 2026-08-08)** — `<exe dir>/userdata/settings.txt` (disclosed platform-config-dir fallback when read-only), flat hand-editable `key = value`, fail-soft PER KEY (a typo loses one setting, names its line, never the whole file). Both `pdfce-cli` and `pdfce-gui` read it at startup. **★ ALL THREE FIELDS NOW CONSUMED, corrected same session (`7e33d52`, 2026-08-08)** — `Pass 51.0` shipped `separations` and `word_gap_ratio` stored-but-unconsumed (R83), caught by the librarian's own independent grep sweep rather than the engineer; both are wired now. `cmyk_intent` is fully wired core→render→cli→gui (see the CMYK row, Fonts & rendering). `separations` reaches the GUI delete path and the CLI's `delete-pages`/`extract-pages`/`split` (new `pageops::extract_with`/`split_with`). `word_gap_ratio` reaches `pdfce-cli extract-text` and the block recogniser — **CLI only, no GUI consumer**. **No CLI flag and no GUI control exist for either** — `userdata/settings.txt` is still the only way to set them. **New standing gate, `tools/check-settings-consumed.py`**: fails the build if any `Settings` field is parsed+written but never read outside the settings module; checks wiring exists, not that it's correct (that's a test's job — `crates/pdfce-render/tests/cmyk_intent.rs`). ~~**No in-app editor exists** — `Settings::save` has no caller anywhere in the workspace; the file is hand-edit-only.~~ **★ BUILT (`Pass 51.4`, `6d63d81`, 2026-08-08) — a File-tab settings window** (not a dock panel — the dock's own "watched vs entered" precedent applies), five subject groups, each disclosing its evidence tier and BYTES-vs-RENDER/EXTRACT radius; edits a working copy, Save is `Settings::save`'s first caller. **`Pass 51.3` (same day, `6d63d81`) wired 8 more fields** (`mask_resample`, `image_minify`, `cmyk_jpeg_polarity`, `unmappable_code`, `actual_text`, `missing_as`, `xref_entry_eol`, `trailing_eol`) — **all 11 `Settings` fields now consumed**, 11/11 per `check-settings-consumed.py`. **Only 1 of 11 (`cmyk_jpeg_polarity`) rests on tier-(c)-or-better evidence AND is followed as shipped** — most defaults are tier (d) reasoned guesses, disclosed as such in the window. Unblocks (does not build) ribbon/keymap/dock-layout persistence, below |

### Export

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [ ]* | **Export vector page content to DXF** (`Pass 52.0`–`52.3` COMPLETE across core/cli/gui, 2026-08-09, `3c4aca4`→`1f4839d`→`d2d03a5`→`0466281`) — lets SOLIDWORKS import pdfce output natively, no Acrobat Pro dependency at all. **Redirected from the operator's original ask** ("make pdfce satisfy SOLIDWORKS' Acrobat-presence check") **to the workflow that actually works**: SOLIDWORKS imports DXF natively; Acrobat was never a requirement of that import, only an incidental converter elsewhere in the usual PDF→Illustrator/Inkscape→DXF chain. Hand-written, no new dependency: R2000 (`AC1015`) — **`AC1009`/R12 was declared first, on a self-reasoned "more conservative" guess that turned out incoherent with the `LWPOLYLINE`/`SPLINE` entities also being emitted; every string-grep test passed anyway, and only parsing the output with `ezdxf` caught it** (widened `R172`). Native `CIRCLE`/`ARC` recognised from PDF's cubic Béziers (never flattened; `SPLINE` fallback for genuinely non-circular curves), closed `LWPOLYLINE` via the close flag, declared `$INSUNITS` (inches default). Page text ships as one `TEXT` entity per run (not per object, not `MTEXT`) on a dedicated `PDFCE_TEXT` layer, never `0` — sourced from `C:\personal_rag\dxf\lesson_20260519_sheet_border_titleblock_furniture.md`'s finding that a layer-`0` title block cannot be filtered out downstream. Three-way disclosure (`skipped_text` operator chose to omit / `unreadable_text` pdfce could not decode / `skipped_images` no raster entity in this subset). **GUI: File ▸ Export ▸ Export DXF…** (`0466281`) — non-modal window, Export disabled until a scale resolves; scale is inferred from the drawing's OWN ce-dimension calibration, **PAGE-SCOPED** (`suggest_scale_for_groups`/`dimension_groups_on_page`, shared by core, CLI and GUI as of this commit — a document-wide inference was a real, shipped-then-fixed defect, not a hypothetical: it could silently export one page at a different page's scale). Three states, distinctly disclosed: **Calibrated** (pre-filled, names the source ce-dimension group, via the shared `ui_text::group_scale_summary` formatter) / **Uncalibrated** (field left empty; explicit 1:1-paper-scale opt-in, never a silent default) / **Conflicting** (radio list, nothing pre-selected; typed override resolves it). Multi-page export via `doc.selected_pages`, one DXF per page, `{stem}_p{n}.dxf`, zero-padded to the widest page number in the run. **CLI now matches** (`c58cca1`, 2026-08-09): `export-dxf --pages all --output-dir out/`, same naming convention, one scale for the WHOLE RUN inferred from the deduplicated union of the selected pages' ce-dimension groups (refused, all-or-nothing, on disagreement — same remedy as the single-page conflict case), disclosures summed once per run rather than once per file. **Does not round-trip to a parametric SOLIDWORKS model** — sketch geometry only, never features or dimensions-as-constraints; a ce dimension exports as DXF geometry, not a SOLIDWORKS smart dimension. `core` = `Pass 52.0`, `cli` = `Pass 52.1` (`export-dxf`) + scale substrate, `gui` = `Pass 52.2`, text-run emission = `Pass 52.3`. *`Acrobat` column: `[ ]` is a high-confidence inference, not `Acrobat_Features`-RAG-sourced — Acrobat has no DXF export of any kind, so `pdfce-acrobat-librarian` was deliberately not dispatched (no parity behaviour exists to source); see `ROADMAP.md`'s matching entry.* |

## Planned, in predicted order

**Order is a best-effort derivation from `ROADMAP.md`'s *Next up*
(closest first), then *Backlog*, respecting the operator's own ★★★
priority sequence and stated demand rankings — it is not a promised
build order, and everything past the first several rows is this
librarian's judgment call, not the operator's.** All boxes below are
unticked unless noted; a `[x]` in *Planned* means "the underlying
model/verb already exists — only the named shell is missing," which is
worth knowing when scoping the work. **The `Acrobat` column is not a
pdfce box** — it says whether Acrobat already has the thing we plan to
build.

**"Scale-aware delete disclosure for a Part-rung deletion" REMOVED from
this table 2026-08-09 — SHIPPED as `Pass 32.1`** (`e85824a`). Folded into
*Implemented* → *Vector objects*' *Move / delete a whole vector object*
row, above — the shipped fix lives on the OBJECT-rung delete disclosure
(`vector_objects_deleted`), the same function this row's own wording
named, so it is the same capability, not a narrower one.

**Pass 47.0–47.4 REMOVED from this table 2026-08-08 — all five SHIPPED same day** (`5f9e68c`/`9d7e7a0`/`88f5097`/`33b86f5`/`40daa18`). Moved to *Implemented* → Shell & UX (47.0 multi-target verbs, 47.1 QAT, 47.2 ribbon legibility + collapse toggle, 47.3 editor chrome) and *Implemented* → Forms (47.4, folded into the existing *CREATE a form field* row). Per the maintenance contract this is a move, not a strike-and-annotate — `FEATURES.md` is not append-only.

**`Pass 48.3` REMOVED from this table 2026-08-08 — SHIPPED same day** (`e6ad48c`). Folded into *Implemented* → Fonts & rendering's *Insert an image into a page as a new XObject* row (`gui` now `[x]`), above. `e6ad48c`'s own commit subject claims `Pass 47.7` in error — see `ROADMAP.md`'s `Pass 48.3` Shipped entry for the collision correction; `Pass 47.7`'s row below (contextual ribbon tab) is unaffected and still unbuilt.

**"DeviceCMYK → sRGB colorimetry" REMOVED from this table 2026-08-08 — SHIPPED same day as `Pass 49.0`** (`edf7c02`). Moved to *Implemented* → Fonts & rendering (new row, above the image-insertion row). A new row, **"Preseparated-file (`/SeparationInfo`) page-set integrity"**, was also added directly to *Implemented* → Document & pages for `Pass 50.0` (`a083f70`, same day) — it was never in this table because it was found and fixed within the same session, per the new standing rule R170.

**`Pass 38.5`'s annotation-deletion + CLI half REMOVED from this table 2026-08-09 — SHIPPED same day** (`0a727bb`). Moved to *Implemented* → Annotations & markup (new *Delete an annotation* row, plus the `cli` box on the *Read an annotation's note text* row). **The row below is `Pass 38.5`'s REMAINDER ONLY** — its two layout items, both already invalidated by `Pass 43.0`'s dock retirement before this ship and unaffected by it.

**`move_nodes` core verb REMOVED from this table 2026-08-09 — its core half SHIPPED same day** (`e1430d8`, filed as `Pass 23.3`'s residual scope). Moved to *Implemented* → Vector objects (new *Move a multi-node selection together* row, `core [x]`). `cli`/`gui` stay unbuilt (R151) — see that row for what's still owed. This also narrows the *Select MULTIPLE nodes*, *decision 028 remainder*, and *Pass 23.0–23.3* rows below, all updated in the same edit. **Addendum, same day: GUI (`6fb7ffb`) and CLI (`dbdd9a6`) both shipped afterward — `Pass 23.3` is now COMPLETE across core/cli/gui; nothing R151-owed remains for this capability. See the row's own text, above, for both.**

**`Pass 32.0` REMOVED from this table 2026-08-09 — its core AND CLI halves SHIPPED same day** (`462fe0e`→`947ea5d`→`5bfb8fc`→`d26d269`). Moved to *Implemented* → Text (new *Delete one text run without deleting every run sharing its text object* row, `core [x]` / `cli [x]`). `gui` stays unbuilt, deliberately unticked, blocked on a `pdfce-ui-specialist` rung-design ruling — see `ROADMAP.md`'s Shipped entry for the four uncalled `pdfce-gui` adapters already sitting in wait for that ruling. **Addendum, same day: GUI (`03c4c0f`) shipped afterward — `Pass 32.0` is now COMPLETE across core/cli/gui; the row's `gui` box now reads `[x]`. Three follow-ups the specialist named but did not build are filed below as new Planned rows** (per-run decoded text in the readout; a cascading delete verb; scale-aware delete disclosure symmetric with the existing subpath gap) — **a fourth (a `PartIndex` newtype) is an internal safety refactor with no capability-facing effect and gets no row here.**

**`Pass 52.0`–`52.3` (Export vector page content to DXF) REMOVED from
this table 2026-08-09 — its GUI half SHIPPED same day** (`0466281`),
completing the family across core/cli/gui (core+CLI+text already
shipped 2026-08-09, `3c4aca4`→`1f4839d`→`d2d03a5`). Moved to
*Implemented* → new **Export** section (after Shell & UX) — see that
row for the full feature description, the page-scoped
scale-inference defect fix (`suggest_scale_for_groups`), and the
R2000/AC1015 correction.

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [ ] | [ ] | [ ] | — | **Pass 47.11 — text-edit retarget bug fix (`5ceb5f8`, 2026-08-08). SHIPPED, but boxes deliberately LEFT UNTICKED — this filing cannot substantiate which of core/cli/gui the fix reaches**, per the "never tick a box you cannot substantiate" rule. An operator-reported defect that arrived mid-session with no prior filed ID; minted `47.11` after a Pass-ID collision was found on an attempted `47.5` re-mint (`47.5` was already claimed for right-click menus — see `ROADMAP.md`'s collision note). The dispatch reaching this filing named the commit and the ID correction but not the defect's mechanism or surface — flagged for the engineer to confirm and for the next filing to tick correctly |
| [ ] | — | [ ] | — | **Per-run decoded text in the Part-rung readout** (named 2026-08-09, `pdfce-ui-specialist`, `Pass 32.0` GUI review). Needs a new core accessor — `TextObject::preview` decodes a whole `BT`…`ET` into one string with no per-run boundary retained, so today's readout can name a run only by ordinal, not by its text (e.g. "RUN #3" rather than "RUN #3, containing '55 5/8\"'"). UNSCOPED, no Pass ID. `cli` `—`: the readout is a GUI-only surface |
| [ ] | [ ] | [ ] | — | **A cascading "delete this and everything after it" verb**, for a chain of text runs each `Inherited` from the one before it, where deleting run N one at a time is refused N times in a row (named 2026-08-09, `pdfce-ui-specialist`). **Blocked on measurement, not design**: the specialist was explicit that real inherited-chain lengths on the operator's CAD file are needed before scoping this, not guessed at. UNSCOPED, no Pass ID |
| — | — | [ ] | — | **`Pass 38.5` REMAINDER — two layout items, both INVALIDATED by `Pass 43.0`, owed a re-scope or a retirement, not an implementation as originally written.** The four-compartment default-height tuning and the four-compartment collapse-state reset both named a layout that no longer exists (`Pass 43.0` retired `Properties`/`Activities` as compartments; the left dock is now Pages + Tool, two compartments). `core`/`cli` `—`: both named items were GUI-only. (Pass 38.1 · 38.2 · 38.4 · 38.5's annotation/CLI half have all shipped — see *Implemented*. Pass 38.3 is the property-4 confirmation, an operator question rather than a capability, so it has no row here) |
| [ ] | [ ] | [ ] | ? | **Note-text authoring for geometric markup** — Pass 6.1 markup never sets `/Contents`, which is why the Comments panel shows "No note text" on every pdfce-authored annotation. Shell-redesign spec §8 item 12; **not scoped to a Pass** |
| [ ] | [ ] | [ ] | **[ ]** | Pass 35.0 — ce-dimension tolerance & tolerance types, SolidWorks-style (None/Basic/Bilateral/Symmetric/Limit/Min/Max) — zero existing representation today. **Next up**: Pass 34.2 built the per-ce-dimension property surface its controls need to live in |
| [ ] | [ ] | [ ] | ? | Pass 35.1 — drag a ce dimension's extension lines to extend/retract |
| [ ] | [ ] | [ ] | — | Pass 33.0 — real fix for reflow's auto-detected wrap width inheriting a prior edit's overflow (disclosure ships today; the fix itself — clamp / median-width / refuse — is undecided) |
| ◐ | ◐ | [ ] | **[ ]** | Decision 028 remainder — Tab/Shift+Tab node cycling; **arrow-key node nudge (items 10/11 — still owed for a SINGLE node, which is why there is nothing for a multi-node nudge to extend; scope it alongside the multi-node move capability, now shipped core+GUI+CLI — see *Move a multi-node selection together*, Vector objects, above)**; readout-row corrections; clickable breadcrumb navigation. (**Pass 26.2 SHIPPED** 2026-08-06; **Pass 26.3** — the grab/hover/live-preview plan items — **code committed `f8bbdd4`, build record owed**. Both moved to *Implemented*) |
| [ ] | [ ] | [ ] | [x] | Pass 22.0 — make ce dimensions and foreign annotations selectable, marqueeable, and deletable from the canvas. **Also gates the Comments panel's click-a-row-to-select-on-canvas** |
| ◐ | ◐ | ◐ | ◐ | Pass 23.0–23.3 — ce-dimension format/units GUI control; re-measure + whole-dimension move; descend into nested form-XObject containers; multi-node select/move/delete. **`Pass 23.3` is now COMPLETE across all three surfaces (2026-08-09): core `e1430d8`, GUI `6fb7ffb`, CLI `dbdd9a6`** — node *selection set* shipped as Pass 41.0, node *delete* as Pass 36.1, multi-node *move* (core verb, GUI drag wiring, and `nodes-move` CLI subcommand) all ship too — see *Move a multi-node selection together*, Vector objects, above. **`cli` on THIS combined row moves `[ ]` → `◐`** for the same reason `core`/`gui` are `◐`: it is now built for 23.3's slice only. `core`/`cli`/`gui` all stay `◐` for the row as a WHOLE because 23.0/23.1/23.2 (format/units control, re-measure, container descent) are unaffected and still fully unbuilt |
| — | — | [ ] | — | Pass 24.0, 24.2–24.5 — remaining ribbon slices (fixed-anchor confirm strip, contextual tool tabs, selection tabs, overflow/collapse, keyboard & accessibility) |
| [ ] | [ ] | [ ] | [x] | `EditSession::insert_pages` — true in-place page insertion (no Pass number yet; filed 2026-08-05, Pass 3.5's ship) — the core command that would let Insert edit the open document instead of always writing a new file |
| ◐ | ◐ | ◐ | [x] | Form field creation/authoring — **IN PROGRESS since 2026-08-07** on the operator's direct instruction (*"get form field creation/editing done next"*, which answers open question **(m)**). **Text, check box, RADIO and choice creation, WITH a name-collision resolver, have shipped** (core+CLI — moved to *Implemented*), **as has field/widget DELETION**: Pass 20.1 PARTIAL, Pass 20.2 + 20.3 PARTIAL, Pass 20.0 + 20.1(completion), **Pass 20.2 COMPLETE** (`69ab966`+`834d256`+`817b268`, 2026-08-07). **Decision 020's F2 is therefore DONE in full** — checkbox, radio and deletion — and **§8.3's positional-`/Opt` item is discharged as a reasoned REFUSAL**, which §8.3 permits as an equal outcome. **`/TU` R105 handling and the `/Tabs` disclosure SHIPPED 2026-08-07 (`50a5461`)** — moved to *Implemented*, above. **Still planned:** `/I`/`/TI`, push buttons (F3's remainder — Pass 20.3 stays PARTIAL), tab order AUTHORING (F4, blocked on a spec-RAG `/Tabs` dispatch — distinct from the disclosure, which is done), the GUI surface (F5, needs a UI-specialist dispatch first), field **property editing** (F6 — where F2's `--defaults-from` was deferred to). **The CLI verb-shape ruling is now MADE (2026-08-07): flat verbs stand, decision 020's `forms add-field --type …` superseded** — so the radio/push-button/deletion verbs will be flat too. **Verb NAMES also ruled 2026-08-07 (decision 020 §0.1): radio is `add-radio-button`** (member-shaped, matching `add-check-box`; `add-radio-group` would misdescribe §6's one-call-per-member design) **and deletion is `delete-field` / `delete-widget`** (`delete` is the house word — five shipped `delete` verbs, zero `remove-*`; forms is a verb-first domain). ~~These are names only — **nothing here is built yet.**~~ **BOTH ARE NOW BUILT** (2026-08-07, Pass 20.2 complete). **The push-button verb name is still NOT ruled, and must not be inferred from `add-radio-button`** — R161 supplies the *shape* (forms is verb-first, so `add-<thing>`), not the word. **Still owed to the engineer:** F0's disposition (owed retroactively / deferred / retired). **★ AMENDED 2026-08-07 (eighth filing): the GUI surface (F5) is NO LONGER wholly planned — Pass 20.5 is HEADED and PARTIAL** (`8a8678e`+`165dd49`): the **creation** surface is built (see *Implemented*), and what stays owed under that same ID is the **GUI deletion surface** and the **per-type detail fields** (multiline, initial state, choice option editor), **both deliberately CUT rather than missed**. **One verification is OWED and is not a formality: the rendered appearance was never visually checked** (the operator was at the machine; a capture would have photographed his desktop) — three defects that session were caught only by looking. **Also newly owed:** the `/MK`-border disclosure the R43 amendment asked F5's pane to carry; the pane now exists and does not carry it. **★ AMENDED AGAIN 2026-08-07 (tenth filing): F5's cut is HALF DISCHARGED** — `fc51786`+`69db1c6` built the **GUI deletion surface** (see *Implemented*), so what stays owed under Pass 20.5 narrows to the **per-type detail fields**, the `/MK`-border disclosure, and the **rendered-appearance verification** (now owed for both halves). **Newly owed:** per-widget rows for a **multi-page repeated field** are labelled from position, not page identity, when `/P` is absent — and no fixture exercises it. **★★ AMENDED 2026-08-07 (twenty-eighth filing, `3d345aa`): EDITING HAS STARTED, AND F6 IS SMALLER THAN THIS ROW SAID.** `rename-field` ships (**Pass 20.6 PARTIAL** — moved to *Implemented*, above); **`--defaults-from <field>` does not, so F6 is HALF DONE.** **And F6 is TWO items, not four:** decision 020 §6 names only `--defaults-from` and `rename-field` — **field move, resize and re-flag are UNSCOPED, not deferred**, established by grepping decision 020 for `resize|reposition|move.*field|re-flag|change.*flag` (16 hits, all of them the `remove-*`→`delete-*` supersession, the Shape A→B promotion's key-moving step, or deletion rules). **They need an engineer scope call** — a slice, a decision-020 amendment, or a named refusal — because closing F6 would otherwise make *"field editing is done"* true of F6 and false of the capability. **Also ruled by that commit:** a rename onto an occupied name **REFUSES** (no merge, no auto-suffix). **★★ AMENDED 2026-08-07 (twenty-ninth filing): THE OPERATOR SCOPED IT, MINUTES LATER, IN ONE SENTENCE** — *"form fields and everything else should be draggable and resizeable"*. **MOVE and RESIZE are now `Pass 46.0` and are NOT this row's work** (see the two new rows above); they are filed as **geometry**, not as form-field authoring, because the same `/Rect` verb serves a highlight and a redaction mark and decision 020 governs neither. **⚠ RE-FLAG STAYS UNSCOPED and stays THIS row's problem** — `/Ff` bit editing has no geometric component, and folding it into a geometry Pass because it sat next to move and resize in one sentence would be scope drift. **`--defaults-from` landed in `247b8fa` the same day** — not yet filed as shipped (no Pass ID assigned to it, no gate figures relayed), so **this row's boxes are unchanged pending that ship-filing**; when it is filed, F6 closes and the *"field editing is done"* hazard above becomes live, with re-flag the only item left behind it. **★★ AMENDED 2026-08-07 (thirty-first filing): IT IS NOW FILED. F6 IS CLOSED, `Pass 20.6` IS COMPLETE, AND THE HAZARD IS LIVE.** `--defaults-from` shipped in `247b8fa` (gate figures relayed: 2187 tests / 0 failed, +6 in `form_field_authoring.rs`, clippy 0) — its capability is folded into the *CREATE* row above. **`Pass 46.0`'s first slice also shipped** (`move-widget`, `fd6eadd` — new *Implemented* row), so **move exists for form-field widgets and for nothing else**. **This row's boxes stay `◐`** because F3's push buttons, F4's tab-order authoring and F5's residue are all still owed — **F6 closing does not close the bucket.** **⚠ RE-FLAG IS NOW THE ONLY ITEM OWED A *SCOPE DECISION* rather than an implementation**, and it is still this row's problem. |
| ◐ | ◐ | [ ] | [x] | **Pass 46.0 — MOVE and RESIZE anything carrying `/Rect`: form-field widgets, markup annotations, redaction marks, links, ce dimensions.** Filed 2026-08-07 on the operator's request, verbatim: *"form fields and everything else should be draggable and resizeable"* — which arrived **minutes after** `ROADMAP.md` recorded move/resize as **UNSCOPED**. **★★ PARTLY SHIPPED 2026-08-07 (`fd6eadd`, `Pass 46.0` PARTIAL): `move_widget` + CLI `move-widget`, FORM-FIELD WIDGETS ONLY, no GUI — see the *MOVE a form-field widget* row under *Implemented*. `core`/`cli` are `◐` for that reason. STILL UNBUILT AND STILL THIS ROW: resize for EVERY family, move for markup annotations / redaction marks / links, the entire GUI (drag + eight-handle box), and both owed sub-decisions. ★ The §12.5.5 warning below is CORRECT for resize and WRONG for move — a translation leaves the extent unchanged, so both scale factors stay 1 and **A** degenerates to a pure translation that carries the artwork for free; regeneration is a RESIZE cost, not a `/Rect`-write cost. ★ Resize is REACHABLE: the four widget generators are already size-parameterised, while markup appearances hold ABSOLUTE geometry and need their `MarkupSpec` rescaled — two jobs, very different sizes.** **What the row said before any of it was built, left as filed:** `git grep "pub fn move_\|pub fn resize\|pub fn scale_\|pub fn set_rect\|pub fn reposition"` over `crates/` returns **six `move_*` verbs, all vector-object or ce-dimension, and ZERO resize verbs of any kind** — widgets and annotations have **neither**. **Not the one-array edit it looks like:** §12.5.5's placement algorithm step (b) maps the appearance box's LL/UR corners onto `/Rect`'s **independently in x and y**, so enlarging `/Rect` **stretches the artwork anisotropically** rather than revealing more of it (*"aspect ratio is not preserved. This is normative, not a bug"*). Since **R43** paints from the appearance stream or not at all, every pdfce-authored annotation has a baked `/AP`, so a **resize is a REGENERATE**, not a `/Rect` write — stretched arrowheads and glyphs otherwise. **Move** is a `/Rect` write only for widget-style `/BBox = [0 0 W H]` appearances (§12.7.3.3). **GUI: a drag needs NO confirm step** — rule 4 as narrowed by decision 024 §4.4 permits direct manipulation that is visible and undoable; the new interaction is the **eight-handle resize box**, and node/subpath/object dragging is shipped prior art. **CLI verb names NOT RULED.** **Owed sub-decisions:** `/Popup` follow-vs-independent, and the `/Link` invisible-click-target disclosure. See `ROADMAP.md`'s *OPERATOR REQUEST 2026-08-07* entry (head of *Next up*) |
| [ ] | [ ] | [ ] | ? | **Pass 46.1 — RESIZE a vector object** (content-stream family). Same operator request. **MOVE is already shipped at every rung** — `move_object` (Pass 9c-min), `move_subpath` (Pass 28.0 core, Pass 36.0 GUI), `move_node` (Pass 26.0/30.0), `move_handle` (Pass 30.1/26.1) — **so this row is resize only**, and saying so is what stops a duplicate build. **★★ BLOCKED 2026-08-07 on `docs/decisions/032-vector-scale-mechanism-wrap-vs-rewrite.md` — STATUS OPEN, options A–D laid out, none recommended, awaiting the operator's ruling.** **Gated on a mechanism decision that determines the data model, not just the implementation:** operand-rewrite (geometry scales, **stroke does not**) vs. a wrapping `q … cm … Q` (**stroke scales too**), because §8.4.3.2 puts line width in **user-space units**; an **anisotropic** `cm` makes stroke thickness **orientation-dependent**, and a **zero line width** — *"the thinnest line that can be rendered at device resolution: 1 device pixel wide"*, ubiquitous in CAD output — **is not a number a scale can act on.** **A named refusal of non-uniform scale on stroked content is a legitimate third outcome** (decision 027). **Text and images are open questions inside this family:** an image XObject is already drawn through a `cm` (§8.9.5), making it the cheapest member; text has four candidate mechanisms (`Tf` size / `Tz` / `Tm` rewrite / wrapping `cm`) that interact with shipped Pass 19.x work. `Acrobat` `?`: **not looked up** — the vector/object-editing bucket carries **zero** `Acrobat_Features` files, so no verdict may be asserted here (never read `?` as absence). **★★ 2026-08-07, from the Inkscape RAG's new `transforms__*` bucket (decision 032): `vector-effect: non-scaling-stroke` HAS NO PDF EQUIVALENT** — PDF's only device-space stroke width is `0 w`, a hairline, so **pdfce can offer non-scaling-stroke as an EDITING BEHAVIOUR, never as a DOCUMENT PROPERTY** (a *persisted* such property is `out_of_scope`); **non-uniform scale + stroke-scaling-off is mathematically UNSATISFIABLE** and the reference distorts silently, so rule 4 requires disclosure or refusal, not a fudge; and the **rounded-corner-radii toggle is RULED `out_of_scope`** — un-implementable, since `re` is sharp and rounded corners arrive as flattened Bézier geometry with no surviving radius |
| — | — | [ ] | ? | Pass 47.5 — right-click context menus, canvas AND Objects tree, entries mapped only to verbs that exist. Objects-tree menu mirrors the canvas set plus "Reveal on canvas". **Blocked on `Pass 47.0`** |
| — | — | [ ] | ? | Pass 47.6 — widgets (+ markup annotations, redaction marks, links, ce dimensions) become canvas-selectable at all; the hit-tester covers page content objects only today. **The named second prerequisite for form-field resize**, alongside `Pass 46.0`'s core resize verb |
| — | — | [ ] | — | Pass 47.7 — one contextual ribbon tab, reduced form: `▸ <armed tool>`, identity + "bring to front" only, tool controls stay in the dock. Six-tab cap survives. **(bg) RESOLVED 2026-08-08 — this reduced form ships; decision 024's full Family-B design is revisited after `Pass 47.6`, not retired** |
| — | — | — | — | ~~Pass 47.8 — R124's planned-treatment control style (reduced opacity + dashed outline + disabled + Pass-ID tooltip) applied to already-named gaps, `add-push-button`'s palette entry first~~ **RETIRED 2026-08-08 — operator ruled "no placeholders" (question (bb)); this ID is never picked up. See `ROADMAP.md`'s R124 `★★ RE-AMENDMENT`** |
| — | — | [ ] | ? | Pass 47.9 — Alt-key KeyTips. Real Office parity, genuinely absent; egui 0.35 has no focus-group primitive, so this is bespoke Alt-state tracking. **P2 — not a driver of "feels incomplete"** |
| [ ] | [ ] | [ ] | ? | Pass 47.10 — Objects-tree editing verbs (today: select + expand only), mirroring each row type's canvas verb set. **P2** |
| [ ] | [ ] | [ ] | ? | **Ribbon customisation + saved/named layout configurations** — drag-and-drop, right-click add tab/section, save and choose between layouts (operator request, filed 2026-08-08 with question (bf)'s answer). UNSCOPED, no Pass ID. **Reverses `ribbon.rs`'s standing "deliberately not built" refusal.** ★ **Its persisted-configuration-store prerequisite is now BUILT** (`Pass 51.0`, `2a1b0df`, 2026-08-08, `userdata/settings.txt`) — UNBLOCKED, not itself built; a saved ribbon layout is its own **document** under `userdata/`, per that store's own grammar note, not a row in the flat settings file. Shares the same prerequisite with the row below (and, unasked, with dock-layout persistence — currently session-only, `main.rs:11290`) |
| [ ] | [ ] | [ ] | ? | **Input/keyboard customisation + saved configurations** — every mouse gesture and keyboard shortcut made rebindable, with savable named configurations (operator request, filed 2026-08-08 with question (be)'s answer, broader than the marquee question it arrived beside). UNSCOPED, no Pass ID. ★ **Prerequisite now BUILT, not built-into** (same `Pass 51.0` store as the row above) — a saved keymap is its own document under `userdata/`, not a flat-file setting |
| [x] | [ ] | [ ] | **[ ]** | FF-C remainder — apply an embedded/subsetted font to existing text (Pass 21.2, `set-font`) |
| [ ] | [ ] | ◐ | **[ ]** | FF-C remainder — Pass 21.3 GUI font-embedding surface. Pass 37.1 shipped its Add-Text half (face picker, refusal-with-remedy, measured embed disclosure). **Still owed:** the `fsType` trust/licence disclosure (R109, owed since Pass 21.0 — core work) and a picker over EXISTING text (blocked on Pass 21.2) |
| [ ] | [ ] | [ ] | [x] | Forms P1 — radio groups, choice fields, flatten, FDF/XFDF in Batch Tools, regenerate-appearances, per-row page jump, canvas highlight (Pass 37.2's own named residuals) |
| [ ] | [ ] | — | — | Rich-text fill: should `pdfce-core`/`pdfce-cli` refuse too? Pass 37.2 guards the GUI only; `fill-field` can still silently discard stored formatting. Undecided — `ROADMAP.md` *Next up* |
| [ ] | [ ] | [ ] | [x] | Forms P2 — click-on-canvas-to-edit, comb cell dividers, `/Tabs` computed tab order, rich-text editing |
| [ ] | [ ] | [ ] | [x] | FF-B — cross-block / cross-page reflow — Acrobat's is cloud-gated and post-2021; a fully offline one exceeds it |
| [ ] | [ ] | [ ] | [x] | Bulleted/numbered list authoring — **awaiting an explicit operator yes/no; do not schedule until answered** |
| [ ] | [ ] | [ ] | ? | FF-I — minimal StructTree/`/ActualText` update on tagged-page text edits (deliberately cut from FF-H; needs its own decision record) |
| [ ] | [ ] | [ ] | ◐ | Vector graphics editing, Inkscape-parity breadth — path booleans (union/difference/intersection/exclusion/division); stroke & fill incl. gradients/patterns; object transforms with numeric entry (move/scale/rotate/skew); align/distribute; z-order; group/ungroup; text-to-path; general-purpose OCG layer authoring (beyond ce-dimension groups) |
| [ ] | [ ] | [ ] | [x] | Encryption — decrypt all legacy handlers, encrypt-on-save AES-128/256 (Pass 5 — spec-grounded, blocked on an AES-256 `/R 5` vs `/R 6` sourcing sub-decision) |
| [ ] | [ ] | [ ] | [x] | Redaction: mark by dragging on the canvas + transient property bar (scope-called out of Pass 8.1, not built) |
| [ ] | [ ] | [ ] | [x] | Redaction: Sanitize / Remove Hidden Information — not yet scoped to a Pass |
| [ ] | [ ] | [ ] | [x] | Digital signatures — PKCS#7 sign/verify, PAdES B-B/B-T/B-LT/B-LTA, RFC 3161 timestamping (lowest real-world demand of the ranked Backlog set — `/SigFlags` in 0.64% of the organic census) |
| [ ] | [ ] | [ ] | [x] | Bates numbering / stamping — header/footer stamps, sequential numbering across a batch, watermarks |
| [ ] | [ ] | [ ] | [x] | PDF/A conformance — convert-to and validate-against PDF/A-1/2/3/4 |
| [ ] | [ ] | [ ] | [x] | OCR — recognize-text-in-scanned-page (engine binding not yet chosen) |
| [ ] | [ ] | [ ] | [x] | Accessibility (PDF/UA) — tagged structure-tree authoring, reading-order tools, alt-text prompts |
| [ ] | [ ] | [ ] | [x] | Comparison — visual + text diff between two PDF revisions |
| [ ] | [ ] | [ ] | [x] | Portfolios (PDF Package) — multi-file container support |
| [ ] | [ ] | [ ] | [x] | Optimization / linearization — Fast Web View, image downsampling, font subsetting on save |
| [ ] | [ ] | [ ] | [x] | Print & prepress (PDF/X) — low priority unless the operator signals otherwise |
| [ ] | — | [ ] | — | Non-Latin UI font coverage — CJK/Arabic/Hebrew glyphs in pdfce's own chrome (bundle a subset vs. runtime system-font discovery, undecided) |
| [ ] | — | [ ] | ? | Autosave / crash-recovery, then true in-place Save (in-place save is deliberately gated on autosave existing first) |
| [ ] | [ ] | — | — | Release & distribution channel — Scoop + WinGet manifests, published checksums (blocked on a separate, not-yet-granted publish authorization — independent of the license decision) |

`◐` = partially shipped; the row names its own split. For the decision-028
row, `ROADMAP.md`'s decision-028 item table has the exact per-item state
(some of the 6+ named sub-items are done, some are not). In the
**Acrobat** column `◐` means something different — Acrobat has only part
of what the row names.

## Cannot

Structurally unavailable to pdfce — facts about its architecture and
legal posture, not editorial choices. Not a to-do list; these will not
acquire rows above.

- **Any Adobe Document Cloud storage/sync feature**, incl. mobile
  companion sync — pdfce has no cloud backend and `ARCHITECTURE.md` §1.1
  forbids undisclosed network calls by design.
- **Adobe Acrobat Sign as a hosted, multi-party e-signature routing
  service** (send-for-signature links, signing order, hosted audit
  trail). pdfce's own PKCS#7/PAdES signing is *not* blocked by this, nor
  is a local Fill-&-Sign equivalent.
- **AEM Document Security / persistent DRM with remote revoke** —
  requires an Adobe-hosted rights server; replicating it is a different
  product.
- **Adobe's specific cloud services** — Sensei-backed enhanced
  auto-tagging, Auto-Adjust Layout, Liquid Mode, AI Assistant. The
  *hosted mechanism* cannot be replicated; the underlying capabilities
  can, offline, and doing so is an exceed.
- **Anything only achievable by linking GPL/AGPL code** — pdfce is MIT
  (`LEGAL.md` §1), so MuPDF/Poppler/Ghostscript are out as shortcuts on
  any row above.

## Will not

Scope decisions already taken, from pdfce's own records — not this
file's judgment.

- **Dynamic XFA** — `out_of_scope` (decision 020); post-Acrobat-8.1 it
  carries no AcroForm, so there is no behaviour to match.
- **Static-XFA hybrid field creation** — refused by name (decision 020):
  pdfce can write the AcroForm half but not the XFA half, and a
  one-sided add makes two viewers disagree on field count.
- **Any network call without explicit, disclosed opt-in** —
  `ARCHITECTURE.md` §1.1, enforced by the fail-closed `no-network` CI
  gate (decision 003, R12). Rules out telemetry and silent update checks
  in their own right.
- **Barcode form fields** — Acrobat gates these to Pro as a narrow
  legal-workflow feature; out of scope here.
